# CCR self-merge 流程測試檔

這是 CCR (Claude Code) PR self-merge 流程測試檔。用來驗證排程觸發的 session
能否透過建立分支 + 開 PR 的方式把改動送回 `main`，而不直接 `git push origin main`。

- 產生時間（UTC）：2026-08-31T02:55:17Z
- 產生時所在分支：`test/ccr-pr-self-merge-probe`（原始分支：`main`）

確認流程成功後可以刪除本檔案。
