# CS230 Autumn 2025 — Lecture 5: Deep Reinforcement Learning

- 影片：https://www.youtube.com/watch?v=4E27qlfYw0A
- 上課日 2025-10-21，長度 1:45:01，43K views
- 講者：Kian Katanforoosh
- Slides：https://cs230.stanford.edu/syllabus/fall_2025/5/lecture_5.pdf

## 這堂的性質

**臨時換掉的一講。** Kian 開場就說：原本這週要講神經網路可解釋性與 LLM 視覺化，
但學生還沒學到 attention map 和 CNN，第五週講會太超前，**所以把可解釋性挪到後面**（就是 Lecture 10）。

結構：前 2/3 是 Q-learning 從零推導，最後 20 分鐘講 **RLHF**。
**後半那 20 分鐘是站上讀者的重點**——它把 RLHF 完整放回 RL 的框架裡解釋，
這是多數 LLM 文章跳過的一步。

## 開場：RL 憑什麼重要（四篇論文的分期）

| 年份 | 成果 | 意義 |
|---|---|---|
| DeepMind | *Human-level control through deep RL* | **單一演算法**打贏 40–50 款 Atari 遊戲的人類水準 |
| 2017 | AlphaGo（DeepMind / David Silver 的 lab） | 圍棋 |
| — | AlphaStar / OpenAI Five | StarCraft、Dota——**多智能體、部分可觀測** |
| 2022 | RLHF 對齊語言模型 | 「與 ChatGPT 同期發表」 |

> 「以前機器學習是很利基的，你得為不同任務訓練一個很利基的演算法。
> 這裡是**一個演算法就能學會幾乎所有 Atari 遊戲**，這相當驚人。」

## 為什麼圍棋不能用監督式學習（現場推導，很好的教學設計）

學生提議：拿職業棋譜，X = 當前盤面，Y = 下一步。然後 Kian 逼他們找出三個致命問題：

1. **狀態空間覆蓋不了**：13×13 每個交點三種狀態，歷史棋譜永遠涵蓋不完
2. **你不知道那步是不是好棋**，卻把它當成好棋在學；而且你只看到**一次性的決策**，
   看不到棋手腦子裡的長期策略——「模型只是在配對 X 和 Y，它**根本沒有策略的概念**」
3. **ground truth 本身沒有良好定義**（最強的論點）：
   > 「就算世界最強的人也不是每天都下出自己最好的棋，而**就算是他最好的棋也不是 ground truth**。
   > 你等於在對著一個本身就偏掉一段距離的目標訓練。你永遠不會比最強的人類更強，
   > 而最強的人類在每一步上都不是最優策略。」
   > 就算找一整個專家小組來決定每一步，ground truth 仍然是 ill-defined。

### RL 的一句話定義（他要學生記住的）

> **「RL 就是做出好的決策序列，而且是自動做到。」**

> 「監督式學習是**用範例教**（teach by example），強化學習是**用經驗教**（teach by experience）。
> 你不是給模型看貓和非貓，你是讓模型**去經歷一個環境**，直到它搞清楚哪些決策是好的，然後從中學習。」

### 應用（學生答，加上他補的那個）

自駕（看到前方紅燈開始減速，但也許會轉綠所以不完全煞停）、機器人控制
（從 A 點走到 B 點要下的關節決策數量極大）、然後是**最大的那個，沒人講出來**：

> 「**廣告。**我不喜歡它，但它剛好是強化學習最大的應用。」
> 理由：買家在下單前會被展示很多次廣告，這是一場**長局**，需要長期思考。
> 所以行銷、廣告、即時競價有大量 RL。

## 詞彙（他建立得很乾淨）

agent / environment / action `a_t` / state `s_t` / **observation `o_t`** / reward `r_t` / transition

### 為什麼 state 和 observation 要分成兩個概念（好問題）

- 圍棋、西洋棋：**observation = state**，資訊全公開
- League of Legends、StarCraft：**有戰爭迷霧**，你只看得到地圖的一部分 → observation 的資訊少於 state
- 學生補充的第三種情況：**開放世界遊戲為了計算考量**故意只觀察一個半徑內的範圍
  ——「你在某個位置，誰在乎你西邊兩萬公里發生什麼事？」

## 「資源回收」玩具例子（Q table 從零手算）

五個狀態、起點 S2、動作只有左右：

```
S1(垃圾桶,+2) ← S2(起點) → S3(空) → S4(巧克力包裝,+1) → S5(回收桶,+10)
```

**額外規則：清潔隊三分鐘後來，每移動一格花一分鐘。**

> 為什麼要加這條規則？學生答：**不然你就在 S3 和 S4 之間來回刷巧克力包裝，永遠不去回收桶。**

### discount（他用一個很自然的類比）

`R = Σ γ^t · r_t`，γ ∈ [0,1]

> 「對人類來說 discount 是什麼？—— **金錢的時間價值**。考慮通膨，你寧願今天拿一塊錢也不要十年後拿。
> 對機器人來說可能是**電量**：某個策略花很多時間，那就得折現，因為機器人一路上會耗電。」

- γ = 1 → 最佳策略「右右右」，總報酬 **11**
- γ = 0.9 → 用**回溯法**逐格往上算：從 S3 出發最好是 `1 + 0.9×10 = 10`；
  從 S2 出發是 `0 + 0.9×10 = 9`。填滿 Q table，遊戲就解完了。

### Bellman 最優方程

```
Q*(s,a) = r + γ · max_{a'} Q*(s', a')
```

> 「叫最優方程是因為**你的最優 Q table 會滿足這條方程**。遊戲解完之後，
> 這條式子套到任何 (狀態, 動作) 配對上都成立。」
> 「這就是我們剛剛做的回溯：**即時獎勵 + 折扣 × 下一狀態能採取的最好動作。**」

### policy

`π(s) = argmax_a Q*(s,a)` — 「查表，看哪個動作的 Q 值最高，選它。就這樣。」

### Q table 的死穴

> 「狀態和動作空間可能大到不行……想想圍棋，你的石頭可以放在盤上任何地方。
> 這個矩陣會大到**不可能使用**。」

**→ 這就是深度學習進場的地方。**

## Deep Q-Learning

把 Q table 換成神經網路（**萬能函數近似器**）：輸入 state（one-hot），
輸出層每個動作一顆神經元。查表變成一次前向傳播。

### 核心難題：沒有標籤，怎麼訓練？

Kian 逼問學生很久（這段教學過程本身值得記錄）。學生依序提出：用獎勵結構、
跑樹搜尋、只探索比較可能的分支、用期望報酬估。他一一指出問題
（「圍棋的樹一開始就 13×13 個選項然後指數成長，**不可解**」）。

**答案：我們只知道兩件事——獎勵結構，以及最終 Q 函數會滿足 Bellman 方程。就用這兩件事造標籤。**

```
loss = L2( Y , Q(s,a) )
其中  Y = r + γ · max_{a'} Q(s', a')     ← 用「現在這個還很爛的」Q 網路去估
```

**兩次前向傳播**：
1. 把 s 送進 Q 網路 → 看兩個動作的 Q 值 → 選最高的執行
2. 拿到 s'，**再送進同一個 Q 網路** → 取最好的 Q 值 → 折現後加上即時獎勵 → 這就是 Y

> 「記住：**這個目標是偏的，它不是完美的目標，但它比沒有好。**
> 不只告訴我們左邊有個好獎勵，還加上我們知道訓練結束時 Q 值應該滿足 Bellman 方程，
> 那何不就把 Bellman 方程當成目標？」

> 「每次你的 Q 變好，**它對下一個狀態也變好了**。所以你的估計和你的模型是**一起變好的**，
> 最後兩者一起收斂到最優方程。**這是個很妙的概念，對吧？**」

**實作細節**：Y 裡面那項技術上有參數、可以微分，但**我們把它當成常數固定住不微分**
（對照監督式學習裡 Y 就是個固定數字）。

**只往前看一步**，不多看：「你可以，但計算量會更重。」

### 虛擬碼（vanilla）

```
初始化 Q 網路參數
for each episode:
    s ← 初始狀態
    while 非終止狀態:
        前向傳播 s → 取 argmax Q 的動作 a
        執行 a → 觀察 r 和 s'
        前向傳播 s' → 算 Y = r + γ·max Q(s',·)
        算 loss → 梯度下降更新參數
```

> 「就算沒全部聽懂也沒關係。**這在 Stanford 是一整門課、一整學期的東西**，
> 我們是想在一個半到兩小時裡抓到基本盤。」

## 實戰：Breakout（打磚塊）

### 輸入該放什麼（一連串很好的取捨討論）

| 決定 | 理由 |
|---|---|
| 整個畫面當 state | — |
| **裁掉分數** | 這遊戲只求贏，分數不影響策略 |
| 裁掉底部 | 球過了球拍就結束了，那幾行像素不用管 |
| **轉灰階** | 這遊戲顏色沒有資訊 |
| **疊四格畫面（history）** | ★ 沒人想到的那個：**單一畫面看不出球往哪走**，遊戲根本無解 |

**但「裁掉分數」不是通則**——他自己補的反例：
> 「我喜歡足球。足球裡如果你 1:0 領先，你可以 park the bus（全員回防）。
> **你的策略取決於比分**——落後的時候你會做完全相反的事，全力進攻。
> 所以有些遊戲你要分數，有些不要。**這是 AI 工程師要判斷的：哪些資訊需要、哪些不需要。**」

**DeepMind 的真實踩雷（很好的細節）**：他們為了跑 50 幾款 Atari 用同一套前處理，
統一把顏色通道拿掉，「結果在某一款——我想是 Seaquest——**魚不見了**，那款遊戲就破不了，
他們得為它做點微調。」

**為什麼不用低維度的遊戲狀態表示？**
> 「你完全正確，如果你只做這一款遊戲的話。但因為我們要用**同一個演算法**跑 50 幾款，
> 人類看的是畫面，我們就給畫面，這樣比較可能擴展。」
（同理，速度向量可以取代 history，但 Space Invaders 那種多敵人的遊戲就得重做前處理。）

## 訓練 RL 的三個關鍵技巧（他強調這些不限 Q-learning）

### 1. 終止狀態

> 「終止狀態的目標**不是 Bellman 方程，就是即時獎勵而已。**
> 你走到終點拿到 10，沒有 Bellman、沒有折現。」

### 2. experience replay（經驗回放）

解決兩個問題：

**(a) 連續畫面高度相關**
> 「球從左上飛到右下，中間很多時間步基本上是同一件事，你在重複訓練沒什麼意義的東西。」
>
> 監督式學習的類比：**先訓一個 batch 的貓、再訓一個 batch 的狗、再一個 batch 的貓
> ——它永遠不會收斂，只會一下偏向貓一下偏向狗。**

**(b) 資料完全沒有重複利用**
> 「你經歷一次就立刻拿它訓練，然後就再也見不到它了……
> 也許你做了一件事拿到超棒的獎勵，**你不想忘記它**，你想定期拿它重訓。」

做法：把每次 transition 存進 replay memory `D`，訓練時**從 D 裡隨機抽 mini-batch**，
而不是訓練剛剛那一步。

進階版 **prioritized sweeping**：依梯度大小加權，讓有洞見的經驗更常被抽到。
> 「你在西洋棋裡下了一步大失誤，你會想**之後再收到那個經驗**，才不會再犯。」

學生的好問題：replay memory 會不會偏向遊戲開局？
→ 多玩幾局就會混入中盤和殘局。而且他補了一個很好的類比：
> 「初學者很會開局但殘局很爛，**因為他們很少有機會玩到殘局**。這種方法就能拿殘局多重訓幾次。」

### 3. exploration vs exploitation（ε-greedy）

**失效示範**（很清楚）：起點 S1，三個終止狀態獎勵分別是 0、1、1000。
隨機初始化後 Q 值是 .5 / .4 / .3：
- 選動作 1 → 拿到 0 → 更新成 0
- 選動作 2 → 拿到 1 → 更新成 1
- 再選動作 2 → 拿到 1，預測正確，沒事要修 → **訓練結束，永遠沒去過那個 1000 的狀態**

**類比（很好用）**：
> 「你每天騎腳踏車穿過校園，有一條愛用路線。你越常走它就越熟——轉彎更快、
> 知道圓環會有多少人、怎麼繞過去比較快。**這是 exploitation。**
> 但也許有另一條你根本沒想到的路，往南而不是往北，可能更好。
> **你永遠不會知道，因為你沒有勇氣或耐心去試。**」

**解法**：以機率 ε（例如 5%）採取隨機動作。
> 「西洋棋的類比是：你偶爾下一步很有創意的棋，**今天可能比較差，但讓你學到東西、長期變強。**」

學生問「把初始 Q 值設成無限大不就好了？」→ 三個都是無限大你就無法決定；
而且獎勵是 1 的話梯度會爆掉。更根本的理由：
> 「**你無法預測獎勵結構長什麼樣**。我們要的是一個能適應各種環境的 agent。」

## 湧現行為（接回 Lecture 2 的伏筆）

- **Breakout**：agent 自己發現可以**在側邊挖隧道把球送到磚塊後面**，這樣過關容易得多
- **相撲（競爭式自我對弈）**：agent 學會**攻擊對方的腳**、**降低重心求穩**
- 自我對弈的做法：「同一個模型複製幾份、用不同初始化，讓它們學；
  其中一個會變得比其他強，**就把它複製給其他所有模型**，再來一輪。
  因為有 ε-greedy 的隨機性，自然會有一個又跑到前面。」

### AlphaGo 那步「看不懂的棋」（本講最好的一段洞見）

他推薦 Netflix 的 AlphaGo 紀錄片，然後解釋那步怪棋：

> 「那步棋對人類非常不直覺，因為**人類被訓練成要最大化勝率**——
> 西洋棋裡我能吃光你的子我就吃光，圍棋裡我能圍多少就圍多少。
>
> **但 agent 只被設定成要「贏」。**它不在乎是贏一目還是贏二十目。
> 而那步棋剛好把 agent 放在一個**以微小差距獲勝**的好位置上。」

## PPO / DPO（快速帶過但講清楚了分界）

| | Q-learning | PPO |
|---|---|---|
| 學什麼 | 學 **Q 值**，policy = argmax Q | **直接學 policy**，更機率式 |
| 動作空間 | 離散 | **連續也可以** |

> 「自駕不是『方向盤往左或往右』，是**轉幾度**，那是連續的。DQN 在這裡不好用，
> 你得把動作切成一格一格，那沒什麼用。這時要用 PPO。」

- PPO 有 **expected advantage** 的概念：不是告訴你這個動作多好，而是**它比你本來會做的好多少**
- 論文出處：Schulman 的 TRPO 和 PPO
- > 「現在更紅的是一年前 **Stanford 出的 DPO**，這門課不會教。」

**獎勵怎麼設計**（學生問）：圍棋可以只有「贏 = 1、其他 = 0」；
西洋棋可以給中間獎勵鼓勵吃子，但也可以完全 end-to-end 只給最終獎勵——
> 「後者比較難訓練，但可能導向更優的策略，因為**你其實可以一子未吃就獲勝**。」

---

# 後半：RLHF（★ 對站上最重要的 20 分鐘）

> 「這是把你記憶中的 **GPT-2 和 ChatGPT** 分開的那個躍進。」

## 預訓練模型的兩個問題（例子舉得很好）

**問題一：網路資料不反映「有幫助」**

| 訓練集裡有的 | 使用者實際會問的 |
|---|---|
| 「deep learning is so cool」 | 「**什麼是 deep learning？**」 |

> 「模型是被訓練來**續寫文字**，不是回答問題。」

**問題二：模型還沒有「好」「有禮貌」「有幫助」的概念**

問：「我的筆電開不了機，我該怎麼辦？」
- 預訓練模型（學自 Reddit / Wikipedia）：「筆電有時候會因為電源問題而無法開機。」
  ——**這不是你問的，你問的是「我該怎麼辦」**
- 你要的：「先檢查充電器有沒有接好、插座有沒有電。如果沒問題，長按電源鍵十秒。
  如果還是不行，可能是電池或主機板……」

## 步驟一：SFT（監督式微調）

- 建一個**全人工**的 prompt–response 資料集（收集真實 prompt 請人回答；也請專家自寫好 prompt 再自答）
- 訓練方式**和預訓練一模一樣**，只是餵的是人寫的 prompt–response 對：
  給「向初學者解釋 deep learning」+「Deep learning is a type of machine learning that uses neural」
  → 期待模型接出「networks」

**兩個缺點：**
1. **資料極貴。**「我記得第一版 InstructGPT 只有 **13,000 組** prompt–response 對，
   而結果居然相當好。」
2. **不太會泛化。** 這是監督式學習，不是 RL——你只給了 13,000 個例子，
   憑什麼相信它能泛化到使用者丟來的沒見過的 prompt？

> **「SFT 教的是**模仿**人類的好行為。關鍵就在這——它是 imitation，不是 preference optimization。」**

## 步驟二：訓練獎勵模型（RM）

- 對同一個 prompt，從 SFT **取樣**三四個不同回答（調 temperature、不總是取 softmax 第一名）
- 請標註者**排序**：B 優於 C、C 優於 A、A 約等於 D（也可以用多種 Likert 量表，不限簡單排序）
- 這批**成對比較**就是 preference data

**RM 的架構**（講得很具體）：
> 從 SFT 初始化，**把最後的 softmax 層拿掉**，換成一個**輸出純量的線性層**當 reward head。

訓練：同一個 prompt 配 response A 和 response B，各得一個預測獎勵，
loss 鼓勵模型**給偏好的回答更高的獎勵**。

**為什麼用模型而不是直接用人？**
> 「因為模型可以**廣泛地**用在各種輸入上，而且從資料的角度**可以規模化**。」

**為什麼這比 SFT 好？**
> 「**問人『這兩個你偏好哪個』比請人自己寫出答案容易太多，也快太多。**
> 你如果用過 ChatGPT，大概被問過『你比較喜歡哪個回覆』。」

## 步驟三：RLHF（把整套詞彙對回 RL）

**這張對照表是本講最值得直接搬進文章的東西：**

| RL 概念 | 在 RLHF 裡是什麼 |
|---|---|
| agent | **正在被微調的語言模型** |
| environment | 所有可能的 prompt 與續寫構成的空間 |
| state | **prompt + 目前已生成的 token** |
| action | **下一個 token** |
| next state | 多了一個 token 的狀態 |
| reward | **獎勵模型給的分數**（人類偏好的代理） |
| **一個 episode** | **一個完整的 prompt→完成生成** |

### 關鍵的稀疏獎勵性質（這點很多文章漏掉）

> 「模型**不是每個 token 都拿到獎勵**，它是在**序列結束、完成生成之後**才拿到，
> 因為獎勵模型當初就是被要求對『prompt + 回應』整體評分的。
>
> 所以**所有中間獎勵通常都是零**，這讓它成為一個**極度稀疏獎勵的 episodic 任務，
> 就像一盤西洋棋**——你只有在結束時才知道自己下得好不好。」

繞回開場那句定義：**RL 就是做出好的決策序列。**

## 結尾：他自己給的反面材料

> 「有一支很好的影片，**四天前**的，是前 Stanford 學生 Andrej Karpathy——他非常有想法、
> 表達也很清楚——在講**為什麼強化學習有時候很糟糕、人腦的運作方式效率高得多**。
> 我鼓勵你們去看那支四分鐘的影片，因為他很清楚地講出了
> **為什麼 RL 到現在仍然不夠好，即使它在很多方面是我們手上最好的工具。**」

（時間點：Lecture 5 上課日是 2025-10-21，往前四天約 2025-10-17。寫文章時要找出這支影片。）

## 可寫成文章的角度（候選）

1. **★ 主打「RLHF 其實是一個 RL 問題」**：那張對照表（agent = 被微調的模型、
   action = 下一個 token、episode = 一次完整生成）+ **稀疏獎勵**那段。
   站上寫了很多 agent，但沒有一篇把 RLHF 放回 RL 框架講。
   前面 Q-learning 的部分只需要留 Bellman 方程和 ε-greedy 當背景。
2. **主打「監督式學習解不了的問題長什麼樣」**：圍棋那三個反駁——尤其
   **「ground truth 本身沒有良好定義」**——這個論證可以直接搬到今天做 LLM eval 的處境
   （你拿人類標註當標準答案，但人類標註本身就不是最優）。
   這條和站上的 evaluator paradox 接得起來。
3. **主打 exploration vs exploitation**：腳踏車路線的類比 + 那個 1000 獎勵永遠到不了的例子。
   短篇，但概念普世。

## 待補

- Karpathy 那支「四天前」的影片要找出來（約 2025-10-17）
- InstructGPT 的 13,000 組數字要查證（Kian 說「我記得」）
- Schulman TRPO/PPO、DPO（Stanford, 2023）、DQN Nature 論文的正式引用
- Seaquest 去色導致魚消失一事，Kian 自己說「我忘了是哪一款」，要查證或標明不確定
- 資源回收那個 Q table 的數字要自己重算一遍
