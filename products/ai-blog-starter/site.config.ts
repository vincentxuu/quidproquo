// ─────────────────────────────────────────────────────────────
//  這是你唯一需要修改的設定檔。
//  改完存檔，網站就會跟著變。其他檔案都可以不用管。
// ─────────────────────────────────────────────────────────────

export const site = {
  /** 網站標題，會出現在瀏覽器分頁和首頁最上方 */
  title: '我的部落格',

  /** 一句話介紹自己，會出現在首頁標題底下 */
  description: '這裡是我寫東西的地方。',

  /** 你的名字，會出現在頁尾和 RSS */
  author: '你的名字',

  /**
   * 你的網址。
   * 部署到 Cloudflare 之後會拿到一個 xxx.pages.dev 的網址，貼在這裡。
   * 之後綁了自己的網域，再改成自己的網域。
   * 結尾不要加斜線。
   */
  url: 'https://example.pages.dev',

  /** 語言，中文填 'zh-TW'，英文填 'en' */
  lang: 'zh-TW',

  /**
   * 社群連結。不想放的就把整行刪掉，或把網址留成空字串。
   */
  social: {
    email: '',
    github: '',
    x: '',
    linkedin: '',
  },

  /** 首頁一頁顯示幾篇文章 */
  postsPerPage: 10,
} as const

export type SiteConfig = typeof site
