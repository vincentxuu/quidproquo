# quidproquo progress

Last updated: 2026-08-09

協定（完整版見 docs/governance/operating-charter.md）：
- 這是 working memory，不是日誌。上限 90 行，`pnpm verify` 會擋。
- 任務狀態實質改變就更新本檔（含 Last updated）。
- 完成或過期的條目移到 docs/progress-archive.md，不要無限堆積。
- 需要人類拍板或大模型 session 的事，登錄到 docs/governance/escalation-queue.md。

Current focus
- Close short-term blog infrastructure gaps from `docs/TODO.md`.
- Keep RAG work incremental; feature flags + evaluation required for any addition.
- TW stock screening: four-criteria framework (see tw-stock-screen skill).

In progress
- Align actual implementation status with `docs/TODO.md`.
- Finish low-risk P1 items: RSS author metadata and font preload.

Next steps
1. Backfill missing post `type` fields with a script + sampling/rollback plan.
2. Backfill missing `tldr` fields.
3. Make `type` required in `src/content.config.ts` — escalation queue item, ask first.
4. Verify accessibility and performance with Lighthouse / axe, then update TODO evidence.

Recently completed
- 2026-08-09: B14 swarm + B12 payload + B16 production ramp (zh+en), 30 -> 34.
  B group is now CLOSED except B13. Findings in plan §5.2 — read that.
  Reusable, in order of how much it cost me:
  (1) TWO AGGREGATES SOLVED SIMULTANEOUSLY IS GUESSING, NOT DERIVATION. B12
  solved two programme totals for unit prices and got them REVERSED and 5x off
  (real: drone set NT$1M, robot NT$6.5M). The line-item procurement record was
  public the whole time. B12 zh+en now carry a correction box; its "2% part"
  line became 11.4%. I HAD flagged "the robot figure looks low" — flagging
  uncertainty is not removing it. When line items exist they are not an
  alternative check, they are the only thing to use.
  (2) THE RECURRING MISTAKE, 6th instance, now closed on the last cell that
  carried it: B16 was filed as "needs factory access". It did not — a ramp's
  public face is lead times and failed tenders, all on the procurement site.
  The general form to ask every time: "does this topic need what I can't get,
  or did I write it to need that?"
  (3) When a clause looks mis-shaped, go find its PARENTAGE (B14: the Control
  Yuan report traced spec Ch.7 to the 2022 National Day chip row).
  (4) Requests-on-record vs delivered-text is a reusable check (B14: a
  2022-11-11 meeting asked for swarm mission-software testing; Ch.7 has none).
  (5) Negative results only count if reproducible — B14's grep hit 16x
  `formation` inside `information`; saying so in print is what made it safe.
  TOOL NOTE: g0v procurement API is now pcc-api.openfun.app (pcc.g0v.ronny.tw
  redirects); responses are huge — parse with a script, never into context.
  Open: B13 only in B; C (applications) and I (social) are whole empty blocks.
- 2026-08-09 (later): audit + four parallel fixes, NO new post.
  (a) B16's CONTROL GROUP now run, and IT REFUTED B16's own first conclusion:
  aerial ladder trucks (mature category) fail-to-award 0.54 vs thermal drones
  0.33. Failed tenders are the NORM in TW fire-equipment procurement, not
  evidence of a thin drone supply side. Section 9 added to both languages.
  THE LESSON, and it is the sharpest one yet: a gap I had honestly listed under
  "what this post does not answer" turned out to REFUTE the post. Listing a gap
  is not handling it. Next time, ask: could X refute what I am writing? If yes,
  X is prerequisite work, not future work.
  (b) B12's RS-column gap FILLED: Taiwan is checked in BOTH RS Column 1 and 2
  (Japan only Column 1), so the 111,000 threshold does NOT gate Taiwan's
  licence requirement at all — it still gates price and product ladder.
  (c) Plan §三 bookkeeping: ✅ marks had only been kept for the B block;
  23 cells were written but unticked. Fixed + coverage note added at §三 head.
  READ THAT NOTE before picking a cell; do not judge coverage from memory.
  (d) Hub now has a READING PATH (6 reader tracks + "if you read only one" +
  a note on the series' corrections). 33 posts had no entry point before.
- 2026-08-08 及更早: drone series 0 -> 30 (zh+en). 全部已歸檔到
  docs/progress-archive.md，per-post findings 在 plan §5.2——查覆蓋看那裡。
  仍然約束我的事實與教訓（不要再犯）：
  * THE RECURRING MISTAKE，六次：把題目預設成需要我拿不到的東西（硬體／訪談／
    工廠／商業機密）。六次全部是假的。四條通則在 plan §6。
  * 主張某個移植忠實 → 拿逐字複製的原始碼 diff 它。
  * 系統推得出來的東西不需要旗標；推不出來的才需要別人告訴它。
  * SITE FACTS：`category` 是容器、`drone` tag 才是主題（tags/[tag].astro 無
    category 過濾），每篇必須把 drone 放第一個；未來日期的文章不會被 build，
    已發佈文章連到更晚日期的文章＝硬 404；frontmatter glossary 會 shadow 站
    台預設；沒有 remark-math plugin，數學寫在 code block。
  * 資安檢測規範現行版是 V2.0（2026-04-30）。
  * 英文文章連到中文 slug 曾是全站性缺陷（163 檔／742 連結），修法的三條安全
    規則在 archive 裡，做任何連結批次改動前先讀。

Notes
- Some TODO entries are stale: English home about, Pagefind integration, OG
  image generation, and part of the redesign already exist in the repo.
- agent-* projects (foundation/evidence/flow/artifact/policy/pipelines-unify/
  console/providers/os): code complete, pending production observation windows.
  Status snapshots in docs/progress-archive.md; pending decisions in
  docs/governance/escalation-queue.md.
