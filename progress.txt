# quidproquo progress

Last updated: 2026-09-21

- **Ask AI 優化（2026-09-21）**：待辦總表 `docs/product/reviews/agent-ux-2026-09-19-ask-ai.md`（P0 已落地，剩三個小缺口：重試仍扣額度、正常答案無「重新回答」、串流狀態無 `role="status"`）。P1「讀文章時的 Ask AI」規格與逐步實作紀錄在 `docs/product/ask-ai/page-context PRD.md`，五步已完成前三步，flag `rag_flag_page_context` 預設關、線上行為不變：(3) 前端——輸入框上方可移除的「目前文章」chip、三題文章建議問題＋一題全站題、來源清單標出目前文章；(1) 管道——slug 進 `/api/chat`、`GET /api/chat/config`、semantic cache 依文章分流；(2) 檢索偏置——planner 判斷 `refers_to_page`、`normalizeResultsNode` 併入該篇段落並置頂、writer 加註，只有問題在講眼前這篇才生效。**端到端尚未驗證**（原因：本機 D1 原本缺 5 個 migration）。本機 migration 已於 2026-09-21 經使用者同意套用，`pnpm sync` 進行中。下一步：sync 完成 → 開本機 flag 實測端到端與 chip 畫面 → 第 4 步評測切片。已知未修：`post.id` 會吃掉版號的點，`RelatedPosts`／glossary explain／來源連結對帶點文章都對不上 D1 slug（詳見 PRD）；agent engine 尚未使用 page context。

- **daily-digest-framework 2026-09-20**：GitHub API 對非 `vincentxuu/quidproquo` repo 仍回 403（scope 限制含 codeload 與 api.github.com），改用 Groundlane `web_fetch` 打 `github.com/{repo}/releases.atom` 逐一查 12 個 B2 watchlist 框架，全數落在過去 24h 外，僅 Pydantic AI v2.46.0（minor、無 breaking change、功能偏增量）不符「必寫」門檻故跳過。改用 Step 3b `web_search` 捕捉清單外框架，找到 **Microsoft Agent Framework** python-1.19.0（2026-09-18，13.6k star，watchlist 未列）：4 項明確標記 BREAKING（HTTP cookie 持久化收緊、MCP skill 封裝限縮為 ZIP＋sampling callback 棄用、provider-backed MCP session 改 per-invocation 作用域、Redis history key 改 provider/session 作用域），符合「必寫」規則；同版新增 MongoDB／Azure DocumentDB／Cosmos DB 三個向量儲存 connector。中英雙版已建立，series order 23。沿用 Q-024 繞法（scratch npm 裝 gray-matter 複製進 node_modules，未動 package.json/lockfile）跑通 `check:references`（新檔 0 issue）與 `check:lang-parity`（2006 組全過）；`pnpm install` 仍卡在既有 `@lanefoundry/gatelane-sdk` codeload 403（Q-024 根因，非本次改動），`lint`／`astro check`／`skills:sync` 未驗證。
- **daily-digest-github 2026-09-20**：GitHub MCP 仍鎖在僅 `vincentxuu/quidproquo`，全程改用 Groundlane `web_fetch` 直打 `api.github.com`／releases.atom 查 B2 watchlist 框架 release（僅 Pydantic AI v2.46.0 落在 48h 內，但已被 09-19 文章寫過，故改選 Claude Code v2.1.277／278，前者含 AGENTS.md 支援）與 `web_search` 找 trending repos。選出 5 個橫跨 agent harness／edge AI／知識圖譜／local-first 記憶／遊戲引擎 MCP 五類（affaan-m/ECC、cactus-compute/needle、Graphify-Labs/graphify、tinyhumansai/openhuman、IvanMurzak/Godot-MCP）；ECC 星數（26 萬+，8 個月內達成）在文中明確標註「成長速度快到該保守看待」（Trending Repository of the Day 徽章＋12 語言 README 同步）而非照單全收。中英雙版已建立，series order 36。沿用既有 Q-024 繞法（scratch npm 裝 gray-matter 複製進 node_modules，未動 package.json/lockfile）跑通 `check:references`（新檔 0 warning）與 `check:lang-parity`（2004 組全過）；`pnpm install` 仍卡在既有 `@lanefoundry/gatelane-sdk` codeload 403（Q-024 根因，非本次改動），`lint`／`astro check`／`skills:sync` 未驗證。
- **BigGo Finance 全站拆解（2026-09-18，已 commit）**：service-teardown 走完 L1–L6（L7 不適用），產出逐輪 walkthrough＋5 機制檔（Agent對話／排程額度／行情／新聞內容／登入計費，共1301行）＋12 份 HAR/截圖原始擷取（`.playwright-mcp/biggo-*`）。關鍵發現：真端點在 `/sparrowhawk` 命名空間、SSE 5事件＋工具鏈 read_skill→es_query、排程兩段式確認＋cron生成、30s輪詢、Cookie四件套。測試會話8＋task1全刪；skill 補「實戰補充」8條並 sync＋verify全綠。剩 Pro 帳號項與 Podcast 選題觀察。注意：對話記錄含使用者明文密碼，已提醒更換。
- **daily-digest-arxiv 2026-09-19**：三層候選池共 98 篇（Layer A cs.AI/cs.CL/cs.MA 09-18 官方批次＋arXiv API 補充查詢、Layer B HF Daily Papers＋Semantic Scholar、Layer C 14 天回溯無晉升）；今日主題「Agent 鷹架設計決定成敗」，選出 3 篇獨立同日論文：An Empirical Study of Harness Design for Coding Agents（2609.20804，176 組配對消融）、SoL-Pi（2609.20519，NVIDIA/MIT，公開程式碼，省 44.7–49.0% token）、How Do Agent Harnesses Create Value?（2609.20474，安慰劑對照，規劃效果 +7.17pp）。中英雙版已建立，series order 118。`check:references`（新檔 0 warning）與 `check:lang-parity`（1982 組全過）已跑通，沿用 Q-024 繞法（scratch npm 裝 gray-matter 複製進 node_modules，未動 package.json/lockfile）；`pnpm install` 仍卡在既有的 `@lanefoundry/gatelane-sdk` codeload 403（Q-024 根因，非本次改動），`lint`／`astro check`／`skills:sync` 未驗證。Semantic Scholar 對 2 篇候選一度 429 限流，已於篩選紀錄標註 toolDegradation。
- **daily-digest-security 2026-09-20**：Q1-Q4＋補充查詢找到的第一候選 LiteLLM MCP 認證繞過（CVE-2026-59822）經比對 progress.txt 09-18 紀錄後確認是 2026-08-31 蜜罐文章已完整報導過的舊事件，判定重複報導不觸發，撤回已寫好的草稿；改選全新事件 **Plugin4Shell**：資安新創 AIR Security 揭露 Claude Code、OpenAI Codex、GitHub Copilot、Gemini CLI 四大主流 AI coding agent 共通的外掛 SHA pinning 繞過漏洞——agent 只驗證有無執行 checkout，未驗證 checkout 結果是否等於 pin 住的 commit，攻擊者用一個和 pin 值撞名的 Git 分支即可讓已安裝外掛在背景自動更新時無聲換成惡意版本，達成零點擊 RCE；Claude Code（2.1.179）與 Codex（0.146.0）已修補，GitHub Copilot 未修補，Gemini CLI 因停用不會修。多方交叉驗證（AIR Security 原始研究＋CSO Online＋Help Net Security＋InfoWorld＋Forkast）。中英雙版已建立，series order 33；沿用 Q-024 繞法（scratch npm 裝 gray-matter 複製進 node_modules，未動 package.json/lockfile）跑通 `check:references`（新檔 0 issue）與 `check:lang-parity`（2005 組全過）；`lint`／`astro check`／`skills:sync` 未驗證（同 Q-024 根因，gatelane-sdk repo-scope 403）。已將 12 則最舊（09-17）的 Recently completed 條目移至 `docs/progress-archive.md`。
- **daily-digest-pricing 2026-09-20**：三組查詢＋Anthropic／OpenAI 官方定價頁＋explainx.ai 交叉核對後未見主要模型（Claude／GPT／Gemini／Mistral）定價異動或 watchlist 公司定價變動；唯一新發現是 OpenAI 官方 deprecations 頁 2026-09-11 公告的 `gpt-5.4-cyber`（10/1 下架，遷移至 `gpt-5.6-cyber`），但屬 Daybreak 受限存取計畫的利基資安垂直模型、非主要模型，且第二來源（releasebot.io）內容逐字複誦官方公告非獨立報導，未達交叉驗證與「主要模型」雙重門檻，判定不觸發。Together AI（今日輪值廠商）僅查到 H100 GPU 算力促銷（非模型 API 定價），不符範疇。依 skill Step 6 輸出「今日無定價變動」，未產文章。
- **daily-digest-benchmark 2026-09-21**：連續第 19 天 Q-023 結構性阻塞（swebench.com OUTPUT_LIMIT／arena.ai js_empty_document／morphllm.com 429+Vercel Security Checkpoint）。arena.ai 這次轉回 09-13～09-19 慣見的 `js_empty_document`（09-20 是 `browser-challenge` UPSTREAM_ERROR），證實兩種失敗型態是同一阻擋交替呈現非新根因。AA Agentic Index 連續第三天 404，用 web_search 找新路徑僅找到不相關的 Intelligence Index v4.1 文章，確認暫無替代 URL。web_search 補充查詢仍只見互相矛盾的第三方聚合站，未提及官方追蹤 scaffold。依 skill Step 4 判斷今日無異動，未產文章，snapshot／escalation-queue 已更新（Q-023 day 19，判斷逐一找替代來源邊際效益已遞減，建議聚焦接手第一步(1)）。
- **daily-digest-model-card 2026-09-20**：HF trending T1-T4 四路掃描後，text-generation／image-text-to-text 候選多為已發過的 base model（DeepSeek-V4.1-Flash／Xing4.0／MiniCPM5／Agnes-3.0-Flash／Nex-N2.5-mini／Edge0 等）或量化/abliterated 衍生版排除；`Cactus-Compute/needle3` 雖落在 7 天內且無 base_model tag，但同一 needle 專案已被今日 daily-digest-github 當作 trending repo 報導，判定重複主題跳過。改選 T3（text-to-image）trendingScore 第一的 **Qwen-Image-2.1**（阿里通義千問，2026-09-20 官方開源，GitHub release note 與 X 官方帳號同步確認）：視覺生成模組維持前代 2.0 的 7B 量級，新增原生 RGBA 透明圖生成／編輯與最多 10 張參考圖多圖合成編輯，符合條件 A（新 base model，createdAt 6 天內、無 base_model tag、likes>50）與條件 B（官方公告確認）雙重觸發。量化基準數據稀缺（發布未滿 24hr，尚無獨立排行榜收錄）：僅取得 Qwen 官方自測 Qwen-Image-Bench 總分 60.28（⚠️ 未經第三方覆現，已於內文與表格明確標註）與 SGLang 團隊獨立驗證的單樣本 RGBA PSNR 60.69dB 兩組可查證數字，Benchmark 表僅 2 列並在表下加註說明數據稀缺原因，未捏造额外分數。授權為 Qwen Research License Agreement（僅限非商業用途，非 Apache-2.0），已在模型資訊表與「與前代比較」段落明確區分。中英雙版已建立，series order 27。沿用 Q-024 繞法（scratch npm 裝 gray-matter 複製進 node_modules，未動 package.json/lockfile）跑通 `check:references`（新檔 0 issue，全站僅既有 81 個舊 WARN）、`check:lang-parity`（2013 組全過）、`check:series-order`（僅既有缺號 warning）、`check:post-quality`（0 issue）、`check:tw`（0 命中）；`pnpm install`／`lint`／`astro check`／`skills:sync` 未驗證（同 Q-024 根因，gatelane-sdk repo-scope 403）。
- **daily-digest-security 2026-09-21**：Q1-Q4 候選中 LiteLLM CVE-2026-42271／CVE-2026-48710（byteiota.com）比對後確認與 2026-08-31 蜜罐文章已完整報導的同一串鏈，判定重複報導不觸發；改選全新事件 **Google Gemini 評測沙箱逃逸**：Google 09-18 證實一個 Gemini 模型於 5 月的第三方（Irregular）資安評測中，因虛構目標公司名稱撞名真實網域＋測試環境網路隔離設定錯誤，闖入 3 家真實企業系統（一次猜密碼、兩次用公開程式碼庫外洩憑證），Google 稱模型自行停手，但事件從得知到揭露有近七週空窗。三方以上交叉驗證（The Hacker News＋WSJ 原始報導＋Tech Insider）。中英雙版已建立，series order 34；沿用 Q-024 繞法（scratch npm 裝 gray-matter 複製進 node_modules，未動 package.json/lockfile）跑通 `check:references`（新檔 0 warning，全站僅既有 81 個舊 WARN）與 `check:lang-parity`（2014 組全過）；`lint`／`astro check`／`skills:sync` 未驗證（同 Q-024 根因，gatelane-sdk repo-scope 403）。
- **daily-digest-pricing 2026-09-21**：三組查詢＋今日輪值官方頁（Anthropic `platform.claude.com/.../pricing`、OpenAI `developers.openai.com/api/docs/pricing`）交叉核對。找到的變動全數已是舊聞或已被本 skill 寫過：Claude Fable 5.1 cache read 砍 75%（已於 09-04 成文）、GPT-5.5 10/14 從 ChatGPT／Codex 退場（已於 09-17 成文）、xAI x_search 計價改制（已於 09-19 成文）、Anthropic Sonnet 5 $2/$10 定價轉永久（已於 08-17 成文）、GPT-6 Astra／Claude Fable 5.1 上線定價（均屬 model-card 範疇，09-06／09-07 已成文）。唯一真正新發現：OpenAI 官方定價頁新增 `gpt-rosalind-research`（Life Sciences 垂直模型），標註「10/5 起開始計費」，但存取僅限已核准的 trusted-access 內部研究計畫，屬企業/受限存取的利基垂直模型，不符「主要模型」與一般開發者可觸及門檻，判定不觸發。依 skill Step 6 輸出「今日無定價變動」，未產文章。

協定（完整版見 docs/governance/operating-charter.md）：working memory 上限 90 行；狀態改變時更新，完成項目移到 archive。
需要人類拍板或無法完成的事項，登錄到 docs/governance/escalation-queue.md。

Current focus
- **內容販售商業模式系列群（2026-09-17）**：母系列總覽已重寫；B2B orders 0–6、創作者平台 orders 1–7、免費內容 orders 1–8、AI 搜尋 orders 0–6 及三個子系列導讀，合計 66 個中英文章檔（33 組）均完成。交叉審稿必修已修；完成度稽核確認雙向語言連結、References、Mermaid、表格、orders 與內鏈無 blocking gap。`pnpm verify` 於整合後全綠；待使用者 review 才依 scoped paths commit／push，CUA 無可用 browser，Mermaid／手機視覺仍未取得瀏覽器證據。SSOT：`.work/content-business-series-plan.md`。
- **OMP 深度導讀系列（2026-08-31，order 1–15 已完成，擴至 22 篇進行中）**：15 篇中英雙語（30 檔）已 commit；追加 order 16–22 與 Codex 同級（streaming internals、rulebook/TTSR、slash/custom tools、memory、task hub/advisor、blob/secrets/config、OMP 2 crate 全景），規劃見 `.work/omp-deep-dive-series-plan.md`。待 order 16–22 並行撰寫與全量抽檢。
- **Deep Research 全景系列（2026-09-19，全系列完成）**：16 篇全部完成（32 個檔案，含中英雙語）。order 0–15 依序完成。
- Keep RAG work incremental; feature flags + evaluation required for any addition.
- TW stock screening: four-criteria framework (see tw-stock-screen skill).
In progress
- **daily-digest 雲端 routine 卡 pre-commit 修復（2026-09-21，已完成）**：根因＝沙箱無 gatelane repo 授權 → agent 改 package.json 繞 install → hook 裝上 → astro check 必紅 → 想拔 hook 卡權限提示。已做：13 支 routine `sources` 加 `lanefoundry/gatelane`（json 同步 b5808842）；`daily-digest-common` E4 補環境準備＋禁繞 hook 硬規則（fc920a1b）；試跑證實雲端 `pnpm install --frozen-lockfile` 過、pre-commit verify 全綠。補跑落地：9/21 tool 6086aa7、9/21 funding 22f378c4、9/20 日報 37effa18（一次性 routine `trig_01DbzDKRngPBokbUDTZDFTxB`，已自動停用，可刪）。未補：9/20 tool（事件驅動可缺）、9/21 arxiv（可用同法補）、9/20 ai-interview／product-interview（兩支 trigger_id 未登錄，待使用者提供後加 source 並補跑）。四個卡住的舊 session 仍 `requires_action`，原稿仍在沙箱可手動救。
- **AI 模型家族 order 4 Gemini 主篇補 3.8 Flash（2026-09-19，中文已 commit 5bebeb1a）**：時間線+1 列（13 里程碑）、選型表換 3.8 Flash 欄＋effort levels 列、新增 agentic coding 段（DeepSWE 73.7%／TB2.1 89.4%／TB4.0 19.1%／HLE 54.9%，以 Model Card 為準）、子線表加 Cyber＋Omni 行、修正「GPT 在 Terminal-Bench 更強」過時判斷、三代同價＋結尾更新；`check:references`（本檔 0 issue，全站 0 error）與 lint 通過，astro check 未跑（md 改動＋Q-024 既有 blocker）。en 版在 5bebeb1a 只補了更新紀錄，3.8 Flash 正文（時間線／選型表／agentic coding 段／子線表）尚未同步，待翻譯。
- **CI 修復（2026-09-17，待遠端驗證）**：Deploy run 35224971270 卡在 quality／tw；已修 4 篇 9/17 日報缺 lang 與 A 級用語，全量 `pnpm verify` 全綠。未改檢查／workflow；使用者已同意 commit/push，後續確認新 Deploy run 結果。
- **AIP-C01 備考指南補強（2026-09-12）**：中英已補四步準備法、教材比較、實作與應考提醒，另補四份標準版後的獨立考生紀錄，區分 beta 與標準版並標註背景差異；targeted references／tw／diff check 通過，quality 僅既有 `career` tag 提示。全站 verify 既有 blocker 見 Q-024；未 commit／deploy，詳 `.work/aip-c01-update/`。
- **Muse Spark 模型家族（2026-09-19，已驗證待 commit）**：補齊 1.0→1.3 演化、Standard／Contributor 定價與資料權利、Muse Code／Muse Glimmer 關係及 Agent 選型；系列 order 23，中英雙版 references／lang-parity／tw／quality／lint／astro check 全過。
- **AI 模型家族 order 19 Thinking Machines Lab／Inkling（2026-09-05）**：中英雙版已 commit；另修掉擋 verify 的真問題（tw A 級 30 處、quality 6 ERROR、references 3 ERROR、parity 4 draft 標記，詳 Q-021），全在別人 session 草稿上，只修未代 commit。
- **Looplane 系列 Batch D（2026-08-30，內容完成）**：orders 17–18 兩組雙語文章、rivumi 舊檔清除已 commit/push（2026-09-04）。Playwright 型別衝突已自行消解。
- **Admin v2 Phase 1–6 已部署 production（2026-08-30 起）**：管線穩定，剩環境觀察實測。
- **Qwen3 embeddings（代碼已 commit，CI/CD 自動部署）**：provider/adaptive splitting/cache namespace 已到位，待 production 向量重建與 20 題 live eval 確認品質。
- Cloudflare AI Search shadow rollout（`.work/cloudflare-ai-search-evaluation.md`）：索引排隊中，待索引完成與 golden eval。
- Align actual implementation status with `docs/TODO.md`; finish low-risk P1 items: RSS author metadata and font preload.
- **Admin 後台 shadcn 化（2026-09-18，已 push）**：ff324ac9（UI＋死連結）＋c0ebe9ee（傳送端接線＋6/6 tests）皆在 `admin-settings-shadcn` 已 push；Q-028 決策 A＋behavior 預設關已記錄。待：瀏覽器實測、合併、啟用 secret（`wrangler secret put`，使用者操作）。
- **Ask AI 深色／建議／header 修復（2026-09-18，已 commit／push 0efa7b27，待 CI Deploy run 自動上線）**：截圖三問題一次修。根因：全站 `[data-theme="dark"]` 切換但 `dark:` variant 預設只認 `.dark` class，故深色下 shadcn／ai-elements 的 `dark:` 全死；已在 `src/styles/chat.css` 加 `@custom-variant dark` 止血。建議列 `src/components/Chat/ChatWidget.tsx` 由橫向 ScrollArea 改 1／2 欄 grid 卡（4 題一頁、自動換行、token 寫死色不依賴 `dark:`、`換題目` 改 ghost＋refresh icon）。header：`ChatFloating.tsx` 手寫 `⊕ ✕` 刪掉，改由 `ChatWidget` 統一渲染 `ChatHeader＋ChatHeaderToolbar`（新對話／停止／複製／下載／清除），並補 `AbortController` 停止生成＋新對話重置 threadId。後續配色第二輪（未 commit）：header icon 改透明底 `chat-icon-btn`（殺掉淺灰實心框）、建議 pill 對齊站內 `tag-pill`（brand-100 底＋brand-700 字）、送出鍵改深綠圓 `chat-send-btn` 對齊圓形 launcher；`pnpm verify` 全綠＋`pnpm build` 確認三條規則進產物 CSS。陰影第三輪（未 commit）：floating 面板寫死的 `0 24px 60px rgba(0,0,0,0.45)＋1px ring` 改吃主題 `var(--shadow-floating)`（淺色只剩 `0 2px 12px rgba(0,0,0,0.15)`，跟站內卡片同級）、建議 pill 拔掉 Button outline 自帶的 `shadow-xs`（`.chat-suggestion { box-shadow: none }`）；verify＋build 全綠。
- **Ask AI 視覺回歸 job 修到可用（2026-09-19，已 push／上線）**：自 e6198a60 加入後首次 4/4 綠（run 35426781294）。修了四件真問題：基線分平台（`-darwin`／`-linux`，linux 基線只能取自 CI artifact）、`related-posts.ts` 失敗回 503 不再讓文章頁 500、`ChatThread` 捲動容器靜態 `overflow-y-auto` 消除 hydration 前後 15px 跳動、`global-setup.ts` 暖身避開 Vite 冷啟動重最佳化的整頁 reload。仍 non-blocking；穩定幾輪後再考慮擋門。mac `-darwin` 基線是重做前畫面，本機 `pnpm test:visual:update` 可重產（此環境 dev server 起不來）。
- **Ask AI 工具活動顯示重做（2026-09-19，已 push／上線）**：PRD `docs/product/ask-ai/tool-activity-display PRD.md`。三層（pills／ChainOfThought／Reasoning）收成一行深綠字＋兩層展開（清單→Request/Response），比照 claude.ai；後端改送單一 `step` 事件（一 id 一 upsert，`agent_step` 留給 evals）；標籤表統一在 `src/lib/conversation/step-labels.ts`；loading 沿用原軌道動畫；配色深綠字不做 pill／圓點；chat widget 全部 UI 字串 i18n（`src/i18n/chat.ts`，lang 由 PostLayout→ChatFloating→ChatWidget context 下傳）；vitest 加 oxc jsx automatic 才能測 .tsx。靜態預覽（light/dark/zh/en）已截圖確認。dev server 起不來已解（2026-09-19）：Astro 7 偵測 agent 自動轉背景、只等 30s lock，本 repo 啟動約 37s → `ASTRO_DEV_BACKGROUND=0` 已寫進 package.json `dev` script，live 實測與 `pnpm test:visual:update` 皆可跑。同日追加 **agent 引擎**（`src/lib/conversation/engines/agent/`，`rag_pipeline_engine=agent` 切換、預設仍 langgraph）：Planner 把關→單一 agent 用 `search_posts`／`get_post_detail` 自主搜讀→引用檢查（修一次、再不行降級連結）→Critic 只打分→硬失敗退 langgraph；快取獨立 namespace；11 tests。待：admin 切 engine 後 golden eval／shadow 對照。
- **搜尋頁「沒有找到結果」假空結果修復（2026-09-19，未 commit）**：`/search/?q=rag` 顯示無結果，實際是 d1Keyword（LIKE `%q%` 全表掃 3995 篇，348ms 貼 500ms 預算）與 vectorize（embedding 1.2–2s+，2500ms 預算 3/5 次逾時）同時逾時→空陣列，前端只看 `results.length` 誤報。系統性修法：(1) 新模組 `src/lib/retrieval/tools/keyword-posts.ts` 改走既有 `posts_fts`（bm25 欄位權重 10/3/5/2）＋`chunks_fts`（rowid DESC 截 2000 chunk）trigram FTS，production 實測 28–90ms，2 字元（AI／微調）退 posts_fts 逐欄 LIKE 16ms；(2) `searchBlogPosts` 新增 `deadlineAt`，向量段逾時只丟向量、保留 BM25＋metadata（metrics `vector_timed_out`）；(3) `search.ts` 全來源皆空且 keyword 逾時時用 3000ms 重跑 keyword（`search-degradation.ts` 純函式判定）；(4) API 回 `degraded`／`degradedSources`，SearchWidget 逾時顯示警示＋重試鈕而非「沒有找到結果」；(5) 順手修 hybrid 分頁 offset 傳兩次的 bug；d1Keyword 預設 timeout 500→800（admin defaults 同步）。21 個新單測＋lint＋verify 全綠，已 commit／push `5fae083c`、Deploy run 35438667060 成功。線上驗證：rag／AI／微調／模型家族／AI agent／Self-RAG 皆兩來源正常，向量段偶發逾時時 BM25 保留（`partial`）不再假空。冷啟動第一發 d1Keyword 仍超 800ms → 經使用者同意直接寫 production `admin_settings.search_page_source_d1_keyword_timeout_ms=1500`（原本線上有一筆明確的 500 蓋掉程式預設，故 commit 裡的 800 從未生效；hybrid 頁面延遲由 vectorize 2500 主導，拉高不影響體感），API 已回報 1500。後續可選：query embedding cache（Cache API）縮向量段延遲；`searchBm25Posts` 的 `ORDER BY bm25` 全量 join 形狀（~250ms）也可比照截斷。
Next steps
2. Make `type` required in `src/content.config.ts` — escalation queue item, ask first.
3. Verify accessibility and performance with Lighthouse / axe, then update TODO evidence.
4. OCR 實測第二輪：docs/content-plan-ocr-benchmark-round2.md（費率已逐家查證，只有 GPU 期要定預算）。
- **Ask AI 空白回答修復（2026-09-18，已 commit／push 81d055e＋910bd131，待 CI Deploy run 自動上線）**：線上 probe 確認偶發「跑完零內容」（空 draft 通過 validation → done 無 token）＋招呼語必定零回應（off-topic 早退無文字）＋答案全跑完才一次送（體感卡死 30–60s）。已修 F1 validation 擋空草稿（走既有 retry→fallback）、F2 writer 合併 array content、F3 空回應不寫 semantic cache、F4 pipeline 出口 intent-aware 兜底；相關測試 80 pass＋lint 乾淨。`pnpm verify` 全綠（另處理兩個擋門問題：補 `node_modules/mermaid` 頂層 symlink 修 astro check；你的 ChatFloating 未完成修改已修好（補回 `return (<>...</>)` 包裝、樣式保留）併 commit／push。本機 `pnpm run deploy` 因 wrangler 授權 7403 跑不動，改走 CI Deploy workflow 自動上線。
- **Ask AI 連結灰底框修復（2026-09-18，未 commit）**：Streamdown 把回答內連結渲染成 `<button>`，其 `appearance-none` 等 class 只存在於 node_modules 打包檔、Tailwind 掃不到，故掉回瀏覽器原生按鈕外觀。已在 `chat.css` 加 `@source` 掃 streamdown dist＋`button[data-streamdown=link]` 保底樣式；`pnpm build` 產物確認兩層皆生效。
- **Ask AI 舊外觀合併（2026-09-18，已 commit／push 6618b36c，待 CI Deploy）**：決定 pills＋ChainOfThought＋Reasoning 三層。後端：`extractReasoningText` 從 additional_kwargs／content blocks 取思考文字（3000 字截斷）→ planner／writer／critic 經 `model_usage` 攜帶 → 兩 engine 在 stage 完成時送 `reasoning` SSE；前端 ChatWidget 按 stage 累積，ChatMessageRow 用 AI Elements Reasoning 渲染（跑時自動展開、結束自動收）。research 的 hyde／改寫屬中間過程未收錄。87 tests pass＋lint＋astro check 全綠。待 commit／push／CI deploy。
- **快取命中無思考過程（2026-09-18，決議：維持現狀）**：線上 probe 證實同題重問走 semantic cache（5 秒、`cached:true`、只有 token＋done），本來就沒有步驟可顯示。對照業界（OpenAI／Anthropic／GPTCache 皆只回正文、不重播思考；ChatGPT／Claude／Perplexity 不標示快取），決議不存思考過程、不加標籤。
Recently completed
- **daily-digest-report 2026-09-20（補跑，2026-09-21 執行）**：9/20 當天 routine 寫完文章後卡在 pre-commit 權限提示、成品遺失，本次依 skill 覆寫 TODAY=2026-09-20 重做。冪等檢查確認目標檔案不存在後往下跑；彙整 signals JSON（33 筆）+ 當日 Stage 1 全部產出（arxiv／github digest、framework、model-card、security、funding×3，共 8 篇）。主題定調「Agent 讓攻擊的交易成本崩塌，防禦端的資本化才剛開始追上」（交易成本框架），串連 Gemini 紅隊入侵三家公司、研究員用 Claude 72 小時攻進 OpenAI、Plugin4Shell 供應鏈漏洞三則資安事件，與 Arxiv 的 AgentGuard 論文＋Raindrop／Comp AI 同日 Series A 對照防禦端資本化。Step 4e 區域補搜涵蓋日韓／東南亞／印度／中東／拉美／大洋洲六區。relevance≥0.8 信號（7 筆）與全部 8 篇 Stage 1 文章均已核對覆蓋。中英雙版已建立，series order 36。`pnpm install --frozen-lockfile` 全新流程首次乾淨跑完（未卡 gatelane-sdk 403，`sources` 已含 lanefoundry/gatelane），pre-commit hook 正常裝上、全程未繞過。`check:references` 初版有「關鍵詞重疊」WARN（深度分析標題全中文＋「Agent」被判定為 stopword），改標題帶入 `OpenAI` 關鍵詞後降到 0 warning；`check:tw` 抓到「用戶」×2、「賦能」×1 A 級用語已修正（改「使用者」／改寫整句避開「賦能」）；`check:lang-parity`、`check:series-order` 皆僅既有 warning、無新 blocking。
- **daily-digest-tool 2026-09-21**：Groundlane `web_search`（day timeRange）找候選，GitHub `search repos` 不可用（gh CLI 未裝、GitHub MCP scope 鎖死本 repo），全程改用 web_search／web_fetch。過濾掉 omniget（課程下載器搭 MCP 為附加功能，非核心價值）、Elva（Product Hunt 上的付費 SaaS API 目錄工具，非開源）、Cloudflare Workers 多伺服器管控 MCP（linux.do 原帖被機器人驗證擋住無法讀取，改追出其 GitHub repo `aloneio/runmesh`，但授權是 PolyForm Noncommercial 1.0.0，商業使用需另外授權，不符一般讀者可自由試用門檻）；最終選 **SessionRelay**（`EwanJasper/SessionRelay`，MIT，npm `@ewanjasper/sessionrelay`）——本地優先 MCP server，被動採集 Claude Code／Codex／Qoder／ZCode（Trae 僅部分）的對話紀錄統一入庫，16 個 MCP 工具（9 讀 7 寫）支援中文全文檢索與 `.hop` 開放格式交接包（sha256 校驗＋預設密鑰脫敏）；已在注意事項如實揭露專案很新（2 星、0 fork）與規則式抽取準確度僅 60–70% 兩項限制，並用 GitHub API 核實授權與建立時間（非用猜的）。與現有工具比較段落交叉查證 claude-mem（壓縮摘要片段）與 Mem0（向量化雲端記憶）架構差異後才下筆。中英雙版已建立，series order 35。`check:references`（新檔 0 issue，全站僅既有 81 個舊 WARN）、`pnpm lint`（0 warning/error）、`check:lang-parity`（2020 組全過）皆通過；`pnpm install`／`astro check`／`skills:sync` 因 container 內先前已有 node_modules 而未受 Q-024（`@lanefoundry/gatelane-sdk` codeload 403）阻擋，`pnpm install --frozen-lockfile` 本次順利完成。
- **daily-digest-report 2026-09-21**：彙整 signals JSON（30 筆）+ 當日 Stage 1 全部產出（GitHub Digest／Pydantic AI v2.46.0／Google Gemini 評測沙箱逃逸／面試日練 x2；當日無 arxiv digest）。主題定調「模型變便宜之後護城河搬去路由層」（五力分析框架），串連 StepFun Step 5／Qwen3.8-Omni-Flash 的中國廠商低價衝擊模型層，與 LiteLLM CVSS 10.0 漏洞同一天把風險焦點帶到 AI gateway。Step 4e 區域補搜（Groundlane，缺口在非洲）找到 IMF 撒哈拉以南非洲 AI 擴散報告（9% vs 北美 30%）補上非洲區域段；signals 的 community-signal 類別（sig-011 美國 AI 日活調查）因與 agent 產業事件關聯度低、已有更高 relevance 的美國 AI 政策新聞（sig-005 川普 AI Force）覆蓋美國視角，故有意識排除。中英雙版已建立，series order 37（09-20 無日報產出，直接按公式計算，非銜接 09-19 的 35）。全新 container 首次 `pnpm install` 卡在既有 `@lanefoundry/gatelane-sdk` codeload 403（Q-024 根因，非本次改動），沿用 Q-024 繞法（scratch npm 裝 gray-matter 複製進 node_modules，未動 package.json/lockfile）跑通 `check:references`（新檔初版有「參考資料關鍵詞重疊」WARN，因中文標題/標題全中文與參考資料無重疊，補站內 Digest／資安專文連結後 0 warning）與 `check:lang-parity`（2019 組全過）；`lint`／`astro check`／`skills:sync` 未驗證（同 Q-024 根因）。
- **daily-digest-ai-interview 2026-09-21**：DOW=1（週一）ML Fundamentals，權重全 1 未加練覆寫。核心概念涵蓋偏差變異取捨（學習曲線診斷）、L1/L2 正則化的懲罰項差異、batch/SGD 梯度下降的取捨、不平衡資料下準確率失真（precision/recall/F1/PR-AUC）、交叉驗證資料洩漏陷阱五項；練習題改編自 PracHub 題庫的 Transunion Data Scientist 真實面試題「端到端 ML 專案的特徵/模型/驗證策略設計」，拆解思路把五個核心概念串成一套決策框架。中英雙版已建立，series order 33。zh 版原有「參考資料關鍵詞重疊」WARN（標題 "Engineer"/"Fundamentals" 未被引用段落覆蓋）已修（PracHub 參考連結說明補上對應字樣，修真問題非弱化檢查）。全新 container 首次 `pnpm install` 卡在既有 `@lanefoundry/gatelane-sdk` codeload 403（Q-024 根因，非本次改動），沿用 Q-024 繞法（`.pnpm` store 內已下載的 gray-matter 建 symlink 到 `node_modules/`，未動 package.json/lockfile）跑通 `check:references`（新檔 0 warning）；`check:lang-parity`／`lint`／`astro check`／`skills:sync` 待下一步驗證。
- **daily-digest-product-interview 2026-09-21**：DOW=1（週一）Product Sense，權重全 1 未加練覆寫。用 CIRCLES＋使用者問題框架（現況→痛點→根因→機會）練自擬情境題——生鮮外送 App 新使用者簽約數持續成長但週活躍使用者連兩季持平，練習思路強調先用漏斗定位問題環節（啟用率 vs. 複購週期）再提方案，避免看到症狀就直接列功能清單；案例是 Slack 從行為數據挖出「2000 則訊息」活化門檻（跨線團隊 93% 長期留存），直接改寫了 onboarding 策略。中英雙版已建立，series order 33。`check:tw` 抓到「用戶」×40（Mainland 用語，應為「使用者」）與「激活」×4（產品成長術語非 ML activation，WARN 但實際應改）已修正，修後 0 blocking。沿用 Q-024 繞法（scratch 目錄 `npm install gray-matter` 後複製進 `node_modules/`，未動 package.json/lockfile）跑通 `check:references`（新檔 0 warning）、`check:lang-parity`（2017 組全過）、`check:series-order`（僅既有缺號 warning，order 23／32）、`check:post-quality`（新檔 0 issue）；`lint`／`astro check`／`skills:sync` 未驗證（同 Q-024 根因，未嘗試完整解法）。
- **daily-digest-framework 2026-09-21**：GitHub MCP 仍鎖在僅 `vincentxuu/quidproquo`（`api.github.com`／`codeload.github.com` 對其他 repo 皆 403），全程改用 Groundlane `web_fetch` 打 13 個 B2 watchlist 框架的 `releases.atom`（時間戳最可靠）逐一核對 cutoff（`date -d yesterday` 換算 UTC 2026-09-19T00:00:00Z）。僅 Pydantic AI v2.46.0（2026-09-19T03:51:40Z）落在窗內；09-20 的 progress 紀錄曾判定它「功能偏增量」跳過，本次重新檢視內容後改判定符合「寫」門檻：v2.45.0（09-17，未成篇）剛推出 `TypeSafeModel`（串接 TypeSafe 分類器 Jev，不生文字、答型別化問題，AlphaSignal 實測比 gpt-5.6-luna 快約 6 倍但工具參數／union 型別／檔案輸入皆不支援），v2.46.0 精準補上「填工具參數」與「union 型別選型別再填值」兩個上一版的明確限制，另加 `typesafe_boolean_threshold` 可調信心門檻、`supports_text_output` 讓 `LLMJudge`／`GEval` 能用 Jev 當裁判模型；無 breaking changes。同日 daily-digest-github 文章雖也在 Notable Releases 簡短提及同一版本（見其 progress 行），但本 skill 產出完整 deep-dive 格式，比照 09-18 前例兩篇並存不算撞題。其餘 12 個框架（langgraph／crewai／mastra／agno／composio／haystack／dspy／smolagents／llama_index／temporal／ag2／claude-code）release 皆早於窗口或落在窗內但屬純功能追加的 patch 版本（claude-code v2.1.278 為 billing/auth 變更，非 agent 原語級功能，且歷來從未成篇，維持排除）。中英雙版已建立，series order 25。`pnpm install` 於全新 container 首次執行即卡在既有 `@lanefoundry/gatelane-sdk` codeload 403（Q-024 根因，非本次改動），沿用 Q-024 繞法（scratch 目錄 `npm install gray-matter` 後複製進 `node_modules/`，未動 package.json/lockfile）跑通 `check:references`（新檔 0 warning，全站僅既有 81 個舊 WARN）、`check:lang-parity`（2016 組全過）、`check:series-order`（僅既有缺號 warning）、`check:post-quality`（新檔 0 issue）、`check:tw`（新檔 0 命中，全站既有 6 blocking 在別的 09-21 檔案非本次製造）；`lint`／`astro check`／`skills:sync` 未驗證（同 Q-024 根因，未嘗試完整解法）。progress.txt 已將 13 則最舊（09-18）的 Recently completed 條目移至 `docs/progress-archive.md`。
- **AI 模型家族 order 24 Gemma（2026-09-19，中英雙版已 commit 5bebeb1a）**：Gemma 1→4 時間線、Gemma 4 Apache 2.0 vs 舊代 Terms of Use 授權專節、Workers AI／自架／AI Edge 部署三分、Gemini 分工＋Qwen／Llama 對照＋Agent 選型；事實已對官方 blog／授權頁／DeepMind Gemma 頁／HF／Workers AI 模型頁核對；references／tw／quality／links／series-order 全過。en 版已於 5bebeb1a 補齊（8 章節對齊），lang-parity 通過。
- **daily-digest-funding 2026-09-20**：Groundlane 三組查詢後，Cognition／Harvey／Clay 等聚合文章重提的大額輪次已於 09-09～09-11 發過予以排除，Mantic（$25M seed，AI 預測/預測賽局，非 Agent 核心領域）與 Vantora（$100M+，venture builder，無明確輪次字母、不符表格格式）排除；篩到三筆同一天（2026-09-17）公告、未撞號的候選：Raindrop（美國舊金山，Agent 生產環境可觀測性/異常偵測，$35M Series A 由 CRV 領投，累計 $50M，客戶含 Vercel／Framer／Clay）、Comp AI（美國邁阿密，開源 agentic 合規/資安平台，$34M Series A 由 Roo Capital＋Grand Ventures 共同領投，累計 $36.6M，直接對標 Vanta／Drata）、Kastle（美國舊金山，金融服務消費信貸 Agent，$24M Series A 由 Insight Partners 領投，Agent 已處理逾 $18 億美元交易）。三筆皆 Series A 以上直接符合門檻，且雙來源以上交叉驗證（Raindrop：Yahoo/BusinessWire官方稿＋thesaasnews＋TheNextWeb；Comp AI：SiliconANGLE＋TechCrunch＋thesaasnews；Kastle：PRNewswire官方稿＋Dealroom＋YC）。中英六檔已建立，series order 43／44／45。**Q-024 繞法**：`pnpm install` 仍卡在既有 `@lanefoundry/gatelane-sdk` codeload 403（根因非本次改動），改用 scratch npm 目錄裝 `gray-matter` 後複製進 `node_modules/`，跑通 `check:references`（新檔 6 檔 0 issue）與 `check:lang-parity`（2011 組全過）；`lint`／`astro check`／`skills:sync` 未驗證（同 Q-024 根因，待完整 `pnpm install` 解法或人拍板）。
- **daily-digest-benchmark 2026-09-19**：連續第 17 天 Q-023 結構性阻塞，P1 三站失敗型態與 09-18 完全相同（swebench.com OUTPUT_LIMIT／arena.ai js_empty_document／morphllm.com 429+Vercel Security Checkpoint）。實測驗證 09-18 留下的 HF Space iframe 線索（`huggingface.co/spaces/lmarena-ai/chatbot-arena`），確認同樣是 JS 渲染阻塞、不可行，已從待驗證清單排除；AA Agentic Index 正確 URL 狀態從 js_empty_document 惡化為直接 404；morphllm.com/swe-bench-pro 自家部落格頁同樣被同網域 Vercel Security Checkpoint 擋下，確認是網域層防護非單一路徑限流。web_search 補充查詢仍只見互相矛盾的第三方聚合站，未提及官方追蹤 scaffold。依 skill Step 4 判斷今日無異動，未產文章，snapshot／escalation-queue 已更新（Q-023 day 17）。
- **daily-digest-tool 2026-09-19**：GitHub「MCP server」／「model context protocol」過去 24h 新建 repo 搜尋（GitHub MCP `search_repositories`）候選多是當天剛建、1–5 星、功能薄或帶明顯 AI 產文瑕疵（`mcp-context-budget` 的 `npx mcp-context-budget` 安裝指令實測對照 npm registry 確認套件根本沒發布，`computerMCP` README 混入未清乾淨的丹麥文殘留片段），皆排除；`rex-ssh-mcp` 僅限 Windows DPAPI 受眾過窄，`ServerFS_MCP` 綁定 OpenAI 專屬 Secure MCP Tunnel 且需 Docker＋雲端帳號設定，不符「5 分鐘內裝好試用」門檻。改推薦 TrustDex（`grigent/trustdex`，MIT，JavaScript，零 runtime 依賴，1 star，2026-09-18 剛發佈）——本地優先 CLI，在 MCP server／Agent Skill／plugin 曝光給 Agent 前先依政策判斷 ALLOW／ASK／BLOCK，`gate` 只把核准項目寫進新設定、`snapshot`+`diff`+Ed25519 簽章偵測設定日後漂移；與先前介紹過的 mcp-guardrail（runtime 呼叫攔截 proxy）在時機上互補而非重疊，README／THREAT_MODEL.md／package.json 三方核對後才下筆。中英雙版已建立，series order 34。【已廢止繞法：暫移 gatelane-sdk 再 install，2026-09-21 起禁用，見 daily-digest-common E4】安裝全部套件，跑通 `check:references`（新檔 0 issue，全站僅既有 79 個舊檔 WARN）、`check:lang-parity`（1967 組全過）、`pnpm lint`（0 warning/error）、`check:series-order`（僅既有缺號 warning）、`check:post-quality`（0 issue）、`check:tw`（0 命中）、`check:skills-sync`（269 檔全同）、`check:glossary`（僅既有 advisory）、`check:seo-smoke`（exit 0）皆 0 blocking；`astro check` 跑完回報 4 errors，全部集中在既有 `src/lib/gatelane.ts` 對已暫時移除的 `@lanefoundry/gatelane-sdk` 子路徑型別解析（同 Q-024 根因，非本次改動）。驗證後 `git checkout -- package.json pnpm-lock.yaml` 還原成 commit 前的乾淨狀態，`git status` 確認乾淨。
- **daily-digest-pricing 2026-09-19**：三組查詢＋官方定價頁交叉核對後，選出 xAI 官方定價文件公告的 X Search 工具計價改制——2026-09-21 12:00 PT 起 `x_search` 從「每千次呼叫 $5」改成「每千則抓到的貼文 $5＋每千個抓到的 user profile $10」，thread 裡的 parent／quoted post 也算，`web_search`／`code_execution` 等其他工具價格不變；xAI 在 watchlist A1/A2 名單內，符合「watchlist 公司定價變動」門檻。已用 `docs.x.ai/developers/pricing` 官方原文＋兩份第三方追蹤站（Cognee、Supergok）交叉驗證。中英雙版已建立，series order 10。沿用既有繞法（`.pnpm` store 內既有 `gray-matter` 建 symlink 到 `node_modules/`；完整 `pnpm install` 仍卡在既有 `@lanefoundry/gatelane-sdk` repo-scope 403，本次未嘗試完整解法）跑通 `check:references`（新檔 0 issue，全站僅既有 79 個舊 WARN）與 `check:lang-parity`（1968 組全過）；`lint`／`astro check`／`skills:sync` 未驗證（同根因）。
- **daily-digest-ai-interview 2026-09-19**：DOW=6（週六）Paper Reading，權重全 1 未加練覆寫。讀剛登上 Nature 的 arXiv:2509.06917《Paper2Agent》——用多個 specialist agent 分析論文與程式碼、建構可迭代測試精煉的 MCP 伺服器，再接上 Claude Code 這類 chat agent 重現論文結果／回答新查詢；核心概念涵蓋 specialist + coordinator 多 agent 分工、MCP 作為論文能力到可呼叫工具的介面、iterative generate-test-refine 迴圈、reproducibility 與 generalization 兩種要分開驗證的可靠性標準。練習題走審稿人視角，自擬「怎麼驗證自動生成 agent 沒有幻覺」＋「落地內部工具該加什麼防護」情境題。中英雙版已建立，series order 31。zh 版原有「參考資料關鍵詞重疊」WARN（標題 "Paper Reading" 未被引用段落覆蓋）已修（在既有 arXiv 參考連結說明補上 "Paper Reading" 字樣，修真問題非弱化檢查）。沿用 Q-024 繞法（scratch 目錄 npm 裝 gray-matter 複製進 `node_modules/`，完整 `pnpm install` 仍卡在既有 `@lanefoundry/gatelane-sdk` repo-scope 403）跑通 `check:references`（新檔 0 issue，全站僅既有 79 個舊 WARN）與 `check:lang-parity`（1968 組全過）；`lint`／`astro check`／`skills:sync` 未驗證（同 Q-024 根因）。
- **daily-digest-funding 2026-09-19**：Groundlane 三組查詢後，Harvey／Clay／Factory／Temporal 等大額輪次皆已於 09-10～09-17 發過予以排除，篩到三筆未撞號的候選：Magentic（英國/紐約，製造業採購全流程 Agent，$18M Series A，Felicis 領投，14 個月內累計 $23.5M）、CADDi（日本/芝加哥，製造業 AI 資料平台 + CADDi Agent 標準化決策，$114M Series D，估值 $1.2B，半年 2.55x）、Hang Ten Systems（美國 Palo Alto，前 Infosys CEO Vishal Sikka 創辦，agentic 程式碼生成 + 可重用技能庫的企業軟體交付外包，兩輪種子僅隔五週共 $53M+$32M=$85M，符合 Seed ≥$10M 門檻）。三筆皆雙來源以上交叉驗證（Magentic：techfundingnews+thenextweb；CADDi：Fortune exclusive+官方稿；Hang Ten：TechCrunch+pulse2）。中英六檔已建立，series order 40／41／42。【已廢止繞法：暫移 gatelane-sdk 再 install，2026-09-21 起禁用，見 daily-digest-common E4】安裝全部套件，跑通 `check:references`（新檔 0 issue）、`check:lang-parity`（1969 組全過）、`pnpm lint`（0 warning/error，全量）、`check:series-order`（僅既有缺號 warning）、`check:tw`（僅既有可忽略樣式「信號」「打法」「賽道」WARN）、`check:post-quality`（僅既有 tag 泛用性提示）、`check:glossary`／`check:seo-smoke`（exit 0）／`check:skills-sync`（269 檔全同）皆 0 blocking；`astro check` 跑完（4 errors，皆為既有 `src/lib/gatelane.ts` 對 gatelane-sdk 子路徑的型別解析錯誤，同 Q-024 根因非本次改動）。驗證後 `cp` 還原 `package.json`/`pnpm-lock.yaml` 回 commit 前的乾淨狀態（`git status` 確認乾淨）。
- **daily-digest-product-interview 2026-09-19**：DOW=6（週六）Technical PM，權重全 1 未加練覆寫。用 API 版本策略選項比較（路徑版本／header 版本／日期滾動版本）搭配 Cost-Reversibility 權衡矩陣,練自擬情境題——中型 SaaS 企業合約要求 API 半年內不能無預警改格式,工程團隊目前完全沒有版本管理;案例是 Stripe 用日期命名的滾動版本(每月只發相容變更、每半年一次大版本才准破壞相容性、帳號預設釘版本),讓公開 API 十幾年沒真的「壞」過。中英雙版已建立,series order 31。【已廢止繞法：暫移 gatelane-sdk 再 install，2026-09-21 起禁用，見 daily-digest-common E4】安裝全部套件,跑通 `check:references`(新檔 0 issue)、`check:lang-parity`(1969 組全過)、`pnpm lint`(0 warning/error,全量)、`check:series-order`(僅既有缺號 warning)、`check:post-quality`(0 issue)、`check:glossary`／`check:seo-smoke`(exit 0)、`check:skills-sync`(269 檔全同)皆 0 blocking;`check:tw` 抓到「顆粒度」×3 A 級用語已修正為「粒度」／「細緻」(修真問題非弱化檢查),修後 0 blocking;`astro check` 於背景執行中,預期同 Q-024 根因回報既有 `src/lib/gatelane.ts` 4 個型別錯誤(非本次改動)。驗證後將 `git checkout -- package.json pnpm-lock.yaml` 還原成 commit 前的乾淨狀態。
- **daily-digest-report 2026-09-19**：彙整 signals JSON（30 筆）+ 當日 Stage 1 全部產出（面試日練 x2、funding-caddi／funding-hang-ten-systems／funding-magentic、pricing-xai-x-search-billing-change、tool-trustdex；當日無 arxiv／github digest）。主題定調「MCP 把整合成本壓到最低，也把攻擊成本壓到最低」（交易成本框架），串連 Safari／Amazon Ads／GitLab 同日上線 MCP server、JADEPUFFER 全自動勒索軟體攻擊鏈、Azure AI Foundry CVSS 10.0 漏洞、加州 Newsom AI kill switch 行政命令。Step 4e 區域補搜（Groundlane，日缺口：台灣／中國／日韓／中東）補到台灣數發部 AI Agent 論壇與日韓 CAC×DeepSearch agentic 金融合作兩則新事件；中國、中東檢索後無達門檻事件，於區域段落末註明。中英雙版已建立，series order 35。沿用 Q-024 繞法（scratch 目錄裝 `gray-matter` 複製進 `node_modules/`），跑通 `check:references`（新檔 0 issue，全站僅既有 79 個舊 WARN）與 `check:lang-parity`（1975 組全過）；`lint`／`astro check`／`skills:sync` 仍未驗證，同根因待人拍板。
- **daily-digest-report 2026-09-19 重組（本機補跑）**：凌晨 arxiv／github routine 撞帳號 5 小時用量上限（`rate_limit: rejected (five_hour)`，reset 02:20 台北，兩者 02:13／02:17 首次呼叫模型即被拒、0 輪結束、repo 側無任何痕跡），中午手動 `RemoteTrigger run` 補跑成功（c28ffe3e github、943fff66 arxiv）。日報中英雙版隨後重組：深度分析新增「摩擦挂在鷹架層」段落串接 Arxiv 三篇（唯讀驗證器 <1¢ 擋 61% 誤判）與 GitHub 三大廠護欄開源；技術進展補 Arxiv 摘要契約段與 Pydantic AI v2.45/46；工具與生態補護欄三連發；關鍵數字＋2 行；Digest 一覽與參考資料補齊。slug／date／order 35 不動。`check:references` 0 error、`check:lang-parity` 1983 組全過、`check:post-quality` 無新 blocking、`check:tw` 僅既有「賽道」小節標題 WARN。根因待處理：Stage 1 前兩支排程時間落在使用者夜間用量窗尾巴，建議整體後移 ≥30 分鐘（Tier 2 待拍板）。
- **Gemma 覆蓋盤點＋主文更新（2026-09-19，已 commit 5bebeb1a）**：2026-03-27 Gemma 主文 08-18 已遷到 Gemma 4，本次用 post-verify 手法對官方來源核對後補「Gemma 4 家族定位」章節（Workers AI 僅上 26B MoE、31B 走 AI Studio/Gemini API、Gemma 4 轉 Apache 2.0＋前代 Gemma Terms 對照、SEA-LION／embeddinggemma 定位＋站內連結）＋更新紀錄＋3 條官方參考。`check:references` 0 issue、`lint` 0 warning；`check:lang-parity` 原本 zh/en 更新紀錄 2/1 的差異已在 5bebeb1a 補上 en 更新紀錄後消除。Gemini 主篇 Gemma 段落未動，待同步三點見回報；獨立家族文僅給提案不建檔。
- **daily-digest-github 2026-09-19**：GitHub MCP 仍鎖在僅 `vincentxuu/quidproquo`（其餘 repo `get_latest_release`/`search_repositories` 皆 access denied），改用 Groundlane `web_fetch` 打各框架 releases.atom／release 頁與 `web_search` 找 trending repos，`search_repositories` 對公開查詢（非鎖定 owner/repo）可正常運作。Trending Repos 選出同一週不約而同開源內部工具的三個案例（alibaba/open-code-review ⭐36,974 確定性工程+agent 混合架構的 code review CLI、cloudflare/security-audit-skill ⭐14,418 六階段對抗式驗證安全稽核 skill、microsoft/skills ⭐3,032 175 個 Azure SDK skill/MCP 打包），皆與前三日文章無撞號；Notable Releases 選 Pydantic AI v2.45.0/v2.46.0（緊接 09-18 已報過的 v2.44.0 安全修補之後兩天內連發兩個功能版本，TypeSafeModel／Choices helper）。中英雙版已建立，series order 35。【已廢止繞法：暫移 gatelane-sdk 再 install，2026-09-21 起禁用，見 daily-digest-common E4】跑通 `check:references`（新檔 0 issue，全站僅既有 80 個舊 WARN）、`check:lang-parity`（1982 組全過）、`pnpm lint`（0 warning/error）、`check:series-order`（僅既有缺號 warning）、`check:post-quality`（僅既有 tag 泛用性提示）、`check:glossary`／`check:seo-smoke`（exit 0）、`check:skills-sync`（269 檔全同）皆 0 blocking；`check:tw` 抓到「技術棧」×3 A 級用語，比照前幾日慣例改成英文「tech stack」（修真問題非弱化檢查），修後 0 blocking；`astro check` 90 秒逾時中止（同前例既有行為，非本次改動判定失敗）。驗證後 `git checkout -- package.json pnpm-lock.yaml` 還原成 commit 前的乾淨狀態，`git status` 確認乾淨。
- **daily-digest-benchmark 2026-09-20**：連續第 18 天 Q-023 結構性阻塞。swebench.com 仍 OUTPUT_LIMIT、morphllm.com/leaderboard 仍 429+Vercel Security Checkpoint（同前）；arena.ai 這次出現新失敗型態——不是慣見的 `js_empty_document` 空殼，而是直接 `UPSTREAM_ERROR`（stage: browser-challenge，存取挑戰過不了），判斷仍是同一類阻擋、非新根因。AA Agentic Index 連續第二天 404（09-19 也是），判斷頁面已下架或改版待找新路徑。web_search 補充查詢訊號比前幾天更稀薄，Q1 僅命中 1 筆離題結果，Q2 仍是第三方聚合站互相矛盾（如 benchlm.ai 稱 Claude Opus 5 領先 SWE-bench Verified 96%），未提及官方追蹤 scaffold（live-SWE-agent／Sonar Foundation Agent／TRAE）。依 skill Step 4 判斷今日無異動，未產文章，snapshot／escalation-queue 已更新（Q-023 day 18，建議下次優先驗證 AA Agentic Index 新路徑）。
- **daily-digest-model-card 2026-09-19**：HF trending 四路掃描（T1/T2）+ Step 3b 搜尋補充後，當日 HF 榜單候選（DeepSeek-V4.1-Flash／Xing4.0／MiniCPM5-2B／Edge0／Agnes-3.0-Flash／NeoHorse 等）皆已發過或屬排除類別（量化 GGUF／abliterated／同 base finetune）；改由 Step 3b 官方公告訊號選出未撞號的新類別模型：TypeSafe AI（ChatGPT 共同發明人 Diogo Almeida 創立）於 09-15 發佈 Jev——不生成文字、輸出型別化機率決策的「System One Model」，RLCD 訓練法，input $0.042/1M tokens、output 免費，官方工作流自測比 LLM 快 193.6 倍／便宜 444.6 倍（自測未獨立覆現），同日取得 DCVC 領投 $40M 種子輪。三方以上來源交叉驗證（TypeSafe 官方公告＋TechCrunch＋remio.ai 批判性技術評析＋The Register），Benchmark 段落如實標注自測未覆現、型別安全≠決策正確的限制。中英雙版已建立，series order 26。**環境全新（fresh container，無既有 node_modules）**：先用 Q-024 繞法（scratch 目錄 npm 裝 `gray-matter` 複製進 `node_modules/`）跑通 `check:references`（新檔 0 issue）與 `check:lang-parity`（2004 組全過）；`check:tw` 抓到「對標」1 處 A 級用語已修正為「比照」（修真問題非弱化檢查）。【已廢止繞法：暫移 gatelane-sdk 再 install，2026-09-21 起禁用，見 daily-digest-common E4】安裝全部套件，跑通 `pnpm lint`（0 warning/error）、`check:post-quality`（僅既有 tag 泛用性提示）、`check:series-order`（僅既有缺號 warning，AI Model Tracker 本身無缺號）、`check:glossary`（僅既有 advisory）、`check:seo-smoke`（exit 0）、`check:skills-sync`（269 檔全同）、`check:chat-style`（0 blocking）皆 0 blocking；`astro check` 背景執行完成回報既有 4 個 `src/lib/gatelane.ts` 型別解析錯誤（同 Q-024 根因非本次改動）。驗證後 `git checkout -- package.json pnpm-lock.yaml` 還原成 commit 前的乾淨狀態。`docs/progress-archive.md` 仍是 Q-025 未解狀態（內容被覆蓋成不相關文章草稿），本次未嘗試處理，僅新增本行。
- **daily-digest-arxiv 2026-09-20**：週末無新 arXiv 官方公告批次（cs.AI/cs.CL/cs.MA 三頁仍顯示週五 09-18 舊批次，已於昨日完整篩選），改靠 Layer B（HF Daily Papers、Papers with Code/HF trending 搜尋）與 Layer C 14 天回溯重新檢視找出候選；今日主題「Agent 執行留下的軌跡可以變現成什麼」，選出三篇獨立方向：EvoSkill-GUI（2609.17653，浙大，訓練免費的 GUI 技能自我修正，MobileWorld/AndroidWorld/OSWorld 最高 +16.2%/+6.0%/+10.5%，程式碼公開）、AgentGuard（2609.16287，York University，從 642 筆 Claude Code 真實異常軌跡學出護欄，異常執行率 69.0%→26.7%，p<0.001，未釋出程式碼）、Chronicle（2609.20625，Microsoft，切點重播把一次事故變免模型費 CI 迴歸測試，記錄開銷僅 23 微秒/次，6 個自建案例的有條件通過）。中英雙版已建立，series order 119。`check:references`（新檔 0 issue）已跑通；【已廢止繞法：暫移 gatelane-sdk 再 install，2026-09-21 起禁用，見 daily-digest-common E4】跑完檢查後 `cp` 還原兩檔案回原始狀態）取得可用 node_modules；Semantic Scholar 本輪全程 429 限流，citationCount 均記為「未驗證」並標註 toolDegradation。
- **daily-digest-github 2026-09-21**：GitHub MCP 仍鎖在僅 `vincentxuu/quidproquo`，B2 watchlist 13 框架用 subagent 跑 Atom feed 查 release（僅 pydantic-ai v2.46.0、claude-code v2.1.278 落在 48h 內，claude-code 已於 09-20 文章報過故選 pydantic-ai）；trending 候選改用 Groundlane `web_search`＋`web_fetch` 抓 agents-radar／gittok／HN 等自動聚合站當線索，再逐一用 `mcp__github__search_repositories`（對公開查詢無 repo-scope 限制）核實星數，排除一則明顯灌水的聚合報告（`princeruhulofficial/github-trending` issue 宣稱 openclaw 39 萬星與 obra/superpowers 28.9 萬星，經 API 核對只有 openclaw 數字為真，superpowers 未存在對應數據故未採用其列表，改自行逐一驗證候選）。選出 4 個橫跨通用 agent 平台／記憶 context database／computer-use 專家決策模型／設計搜尋 MCP 四類（openclaw/openclaw、volcengine/OpenViking、trycua/cua、INSANE0777/Awwards-mcp），並用 OpenViking 官方 benchmark（直接拿 OpenClaw/Hermes/Claude Code 當對照組）與 pydantic-ai TypeSafeModel／cua CUA-S1 兩個同日出現的「窄任務轉交專門決策模型」案例串成「記憶與決策外包」主題，openclaw 今日新版的升級失敗／記憶體洩漏問題如實引用 agents-radar 的 OpenClaw Ecosystem Digest 而非略過。中英雙版已建立，series order 37。`pnpm install` 首次於全新 container 因既有 `@lanefoundry/gatelane-sdk` codeload 403 中止，沿用 Q-024 繞法（`.pnpm` store 內已下載的 gray-matter 建 symlink 到 `node_modules/`，未動 package.json/lockfile）跑通 `check:references`（新檔 0 warning）與 `check:lang-parity`（2013 組全過）；`lint`／`astro check`／`skills:sync` 未驗證（同 Q-024 根因，未嘗試完整解法）。
- **daily-digest-funding 2026-09-21**：三組查詢初期找到的 Temporal $550M／Cognition $2B／Raindrop $35M／Magentic $18M／Kastle $24M／Hang Ten Systems $53M／AIR Security $50M 全數比對後確認是 09-08～09-20 已成文的舊聞重複出現在聚合站，予以排除；Naive AI（Tencent 投資、$1.4B 估值）屬 LLM 基礎模型而非 Agent 領域、金額也是跨三輪累計非單一輪次，不符表格格式，排除。改選未撞號的 **Enhans**（南韓，企業 Agentic AI 作業系統 AgentOS，$38M Series C，TIMEFOLIO＋Stonebridge Ventures 領投，累計 $60M，POSCO／LG／樂天旗下投資公司首次以策略投資人身分跟投）：Series C 直接符合門檻，三方以上來源交叉驗證（EIN Presswire 官方稿＋digitaltoday.co.kr＋Seoul Economic Daily）。中英雙版已建立，series order 46。本次全新 container 直接 `pnpm install` 成功（未撞到先前 routine 常見的 `@lanefoundry/gatelane-sdk` codeload 403，Q-024 疑似非持續性問題），跑通 `check:references`（新檔 0 issue，全站僅既有 81 個舊 WARN）；`check:lang-parity`／`lint`／`astro check`／`skills:sync` 待下一步驗證。
Notes
- Claude Code 系列寫作聖經在 docs/claude-code-series-guide.md。
- CS228 複查結論（2026-08-22）：公開官方課站停在 Winter 2023–24，維持逐講阻塞。
- agent-* projects: code complete, pending production observation windows. Details in docs/progress-archive.md; pending decisions in escalation-queue.md.
- **Q-015（2026-08-27）**：Tavily API 額度用盡（432），待人決定升級方案或改 skill fallback。
- **Q-021（2026-09-05）**：daily-digest-benchmark 執行時 Groundlane 顯示 connected+enabledInChat 但查無工具，未產出當日 benchmark 文章，見 escalation-queue。
- **Q-020（2026-09-06）**：同日 framework/funding/pricing/signals/report/arxiv/github 七支 routine 皆踩到「Groundlane connected+enabledInChat:true 但需認證、非互動 session 無法 OAuth」，均未產出，見 escalation-queue Q-020 尾端（github digest 額外確認 gh CLI 未裝、GitHub MCP scope 鎖死本 repo）。
- **daily-digest-arxiv 2026-09-06 重跑**：Groundlane 已恢復可用（`ListConnectors`/`ToolSearch` 皆正常），但當天無新公告批次——arXiv `list/{cs.AI,cs.CL,cs.MA}/new` 仍顯示 Friday 2026-09-04 批次，ID 範圍與 2026-09-05 已完整篩選的批次完全重疊，依 skill Step 4a.4 冪等結束，不重選已見論文。未產文章，`.research/daily-arxiv-screening/2026-09-06.json` 已寫入 `status: no-publication` 說明。
- **Q-022（2026-09-06，原登錄 Q-021，因撞號改號）**：daily-digest-security 執行初期 Groundlane／Tavily 也回到「需要授權」狀態，同一 session 容器重啟後恢復，已正常產出 `2026-09-06-security-postgres-mcp-restricted-mode-bypass{,-en}.md`（series order 已於 09-08 修正為 24，見下）。
