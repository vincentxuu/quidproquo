# quidproquo progress

Last updated: 2026-08-08

協定（完整版見 docs/governance/operating-charter.md）：
- 這是 working memory，不是日誌。上限 90 行，`pnpm verify` 會擋。
- 任務狀態實質改變就更新本檔（含 Last updated）。
- 完成或過期的條目移到 docs/progress-archive.md，不要無限堆積。
- 需要人類拍板或大模型 session 的事，登錄到 docs/governance/escalation-queue.md。

Current focus
- Close short-term blog infrastructure gaps from `docs/TODO.md`.
- Keep RAG work incremental; feature flags + evaluation required for any addition.
- TW stock screening: four-criteria framework (see tw-stock-screen skill).

In progress
- Align actual implementation status with `docs/TODO.md`.
- Finish low-risk P1 items: RSS author metadata and font preload.

Next steps
1. Backfill missing post `type` fields with a script + sampling/rollback plan.
2. Backfill missing `tldr` fields.
3. Make `type` required in `src/content.config.ts` — escalation queue item, ask first.
4. Verify accessibility and performance with Lighthouse / axe, then update TODO evidence.

Recently completed
- 2026-08-08: drone series 23 -> 30 (zh+en). Opened the B (technical) block,
  which was entirely empty: B8 flight stacks, B10 RF link, B9 GNSS-denied —
  all three modules the cybersecurity spec names. Archived; findings in §5.2.
  THE RECURRING MISTAKE, 4th instance, now closed: "technical topic" was
  silently filed under "needs hardware". All three were codebase questions;
  the real barrier was Python deps. B9 was the series' first INTERVENTIONAL
  experiment (inject a GPS jam in SITL, record what happens).
  Two reusable rules: (a) to claim a port is faithful, diff it against the
  verbatim original — caught nothing, but that is what makes the claim safe;
  (b) what a system can infer needs no flag; what it cannot is what someone
  else must tell it (why PX4 ships spoofing on and jamming off).
  B17 (spec), B11 (endurance), B15 (airframes) and the 5.8 GHz video-link post
  followed; all archived to docs/progress-archive.md, detail in plan §5.2.
  The video-link post is a NEW SHAPE of finding: the first four were "the
  clause's name doesn't match what it governs"; this one is "no clause is
  shaped like this at all" (analogue FPV fits neither LP0002 §4.10, hopping
  only, nor §5.7, wideband digital only). It also turned up an INDEPENDENT
  CONFIRMATION: a 110-03-16 CAA interview record inside an IOT study says
  flight control and RF are Taiwan's supply-chain weak links and domestic firms
  mostly run PX4/ArduPilot — what B8 and the supply-chain post derived from
  source and filings, written down by the regulator in 2021.
  Four more things worth
  keeping here. (1) VERSION: the cybersecurity spec in force is V2.0
  (2026-04-30), not V1.0 of 2024-12 — the spec-sheet post was corrected.
  (2) B11 and B15 each OVERTURNED an assumption with an equation rather than
  data: weight thresholds do NOT cap endurance (payload does), and fixed-wing
  range is INDEPENDENT of cruise speed. (3) Taiwan has a domestic aircraft at
  EVERY rung of the endurance ladder — that corrects an impression the earlier
  posts may have left that it is all multirotors. (4) SITE FACT: no remark-math
  plugin — `$$` renders literally, write maths in code blocks.
  B15 did NOT need hardware either, so of the two cells flagged as plausibly
  needing it only B16 (manufacturing ramp) is left. Open: B12, B13, B14, B16.
  Also fixed a site-wide defect: English posts linked to zh slugs (163 files /
  742 links). Archived — read the three safety rules there before a link sweep.
- 2026-08-07: drone series 13 -> 23 (zh+en) — D line, B5 financials, counter-UAS,
  four application posts, privacy. Full entry archived to
  docs/progress-archive.md; per-post findings in plan §5.2.
  THE RECURRING MISTAKE (3x here, 4th on 08-08; instances archived): treating
  my own pre-writing "素材" estimate as a hard constraint. Four-rule 通則 in
  plan §6; short form: ask which HALF needs material, ask whether the topic
  really needs it or I wrote it to need it, ask whether it is truly
  unavailable or I just haven't looked, and don't treat interviews as
  higher-grade than public records.
  Application/social cells still open: surveying; noise/NIMBY, criminal misuse,
  autonomous-weapons ethics, environmental cost.
- 2026-08-06: drone series — 13 posts shipped (zh+en). Plan + per-post findings
  + verification lessons live in docs/content-plan-drone.md §5.2; read that, not
  this. Decisions that still bind: `category` is the container (folder/URL/nav),
  `drone` tag is the topic — src/pages/tags/[tag].astro has NO category filter,
  so /tags/drone is the cross-category hub and EVERY series post must carry
  `drone` as its first tag. Investing posts: framework-and-risk, no tickers.
  Future-dated posts are NOT built, so a published post linking to a later-dated
  one is a hard 404 (guard script in plan §5.2). Frontmatter glossary is
  prepended before DEFAULT_GLOSSARY_TERMS and deduped first-wins, so it SHADOWS
  the site-wide entry.

Notes
- Some TODO entries are stale: English home about, Pagefind integration, OG
  image generation, and part of the redesign already exist in the repo.
- agent-* projects (foundation/evidence/flow/artifact/policy/pipelines-unify/
  console/providers/os): code complete, pending production observation windows.
  Status snapshots in docs/progress-archive.md; pending decisions in
  docs/governance/escalation-queue.md.
