# quidproquo progress

Last updated: 2026-08-07

協定（完整版見 docs/governance/operating-charter.md）：
- 這是 working memory，不是日誌。上限 90 行，`pnpm verify` 會擋。
- 任務狀態實質改變就更新本檔（含 Last updated）。
- 完成或過期的條目移到 docs/progress-archive.md，不要無限堆積。
- 需要人類拍板或大模型 session 的事，登錄到 docs/governance/escalation-queue.md。

Current focus
- Close short-term blog infrastructure gaps from `docs/TODO.md`.
- Keep RAG work incremental; feature flags + evaluation required for any addition.
- TW stock screening: four-criteria framework (see tw-stock-screen skill).

In progress
- Align actual implementation status with `docs/TODO.md`.
- Finish low-risk P1 items: RSS author metadata and font preload.

Next steps
1. Backfill missing post `type` fields with a script + sampling/rollback plan.
2. Backfill missing `tldr` fields.
3. Make `type` required in `src/content.config.ts` — escalation queue item, ask first.
4. Verify accessibility and performance with Lighthouse / axe, then update TODO evidence.

Recently completed
- 2026-08-07: drone series expanded 13 -> 23 posts (zh+en). Ten new: spec-sheet
  reading, crash anatomy, CAA question-bank (the D line, unblocked by dropping
  the "must buy a drone" premise); B5 tender financials; counter-UAS; the
  application block's ag-spraying / disaster-SAR / inspection / logistics (four
  value logics: cheaper / no denominator / cheaper+safer / only-thing-available);
  plus the privacy piece closing the series' biggest VANTAGE-POINT gap (22
  pieces all written from the operator side, none about people flown over).
  Per-post findings are in plan §5.2 — read that, not this.
  THE RECURRING MISTAKE (3x): treating my own pre-writing "素材" estimate as a
  hard constraint. B2/B4 "needs interviews" (false — open stack + public JDs),
  D1-D3 "needs buying a drone" (false — I had written the topics to need it),
  B4's margin/cash-cycle "needs interviews" (false — filing questions, and I had
  already run that method on Garuda in A3). Four-rule 通則 now in plan §6; short
  form: ask which HALF needs material, ask whether the topic really needs it or
  I wrote it to need it, ask whether it is truly unavailable or I just haven't
  looked, and don't treat interviews as higher-grade than public records.
  Claims to handle carefully if reused: registered-drone count
  75,240->40,134->38,683 is an INFERENCE (Art.10 two-year validity), not a
  finding; the "rice only, 5 chemicals" drone-spray approval is a 2021 baseline
  — check APHIA systems before quoting as current.
  Investing-line boundary settled: named-company public filings are fine as a
  worked example, zero buy/sell judgment (user approved this reading).
  TWO PLANNING ASSUMPTIONS CORRECTED, both about BVLOS. Inspection isn't
  blocked — it SEGMENTS work into within-line-of-sight units. Logistics isn't
  either — CAA approved 10 cases / 24 corridors, IOT ran PoC->PoS->PoB from
  2020, routing around by getting corridors approved ONE AT A TIME, which fails
  for inspection (demand spread over 22k bridges) and works for logistics
  (demand on a few routes). Lesson: ask whether the regime's GRANULARITY fits
  the demand's SHAPE, not whether a framework exists.
  PRIVACY findings worth reusing: the Legislative Yuan's own 2020 report admits
  the drone chapter has NO privacy provision (it is flight-safety and national-
  security law); the AirTag "dragnet surveillance" doctrine — public movements
  aggregating into a non-public picture — is ready-made for drones but unused,
  and is the same REPETITION property the inspection piece treats as a virtue;
  the real bottleneck is evidence, and CAA's flight-logging system is closed to
  the public.
  Cells still open (nothing hard-blocked): surveying; noise/NIMBY, criminal
  misuse, autonomous-weapons ethics, environmental cost.
- 2026-08-06: drone series — 13 posts shipped (zh+en) across tech/policy/career/
  investing/product/education. Plan + per-post findings + cross-post
  verification lessons live in docs/content-plan-drone.md §5.2 (read that before
  continuing; it is the record, this is only working memory).
  Structural decisions that still bind: `category` is the container
  (folder/URL/nav), `drone` tag is the topic — src/pages/tags/[tag].astro has
  NO category filter so /tags/drone is the cross-category hub, and EVERY series
  post must carry `drone` as its first tag. Investing posts go
  framework-and-risk only, no tickers.
  Scheduling: future-dated posts are NOT built, so a published post linking to a
  later-dated one is a hard 404; guard script in plan §5.2.
  Glossary frontmatter is prepended before DEFAULT_GLOSSARY_TERMS and deduped
  first-wins, so a frontmatter entry SHADOWS the site-wide one.

Notes
- Some TODO entries are stale: English home about, Pagefind integration, OG
  image generation, and part of the redesign already exist in the repo.
- agent-* projects (foundation/evidence/flow/artifact/policy/pipelines-unify/
  console/providers/os): code complete, pending production observation windows.
  Status snapshots in docs/progress-archive.md; pending decisions in
  docs/governance/escalation-queue.md.
