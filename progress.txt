# quidproquo progress

Last updated: 2026-08-09

協定（完整版見 docs/governance/operating-charter.md）：
- 這是 working memory，不是日誌。上限 90 行，`pnpm verify` 會擋。
- 任務狀態實質改變就更新本檔（含 Last updated）。
- 完成或過期的條目移到 docs/progress-archive.md，不要無限堆積。
- 需要人類拍板或大模型 session 的事，登錄到 docs/governance/escalation-queue.md。

Current focus
- Close short-term blog infrastructure gaps from `docs/TODO.md`.
- Keep RAG work incremental; feature flags + evaluation required for any addition.
- TW stock screening: four-criteria framework (see tw-stock-screen skill).

In progress
- Align actual implementation status with `docs/TODO.md`.
- Finish low-risk P1 items: RSS author metadata and font preload.

Next steps
1. Backfill missing post `type` fields with a script + sampling/rollback plan.
2. Backfill missing `tldr` fields.
3. Make `type` required in `src/content.config.ts` — escalation queue item, ask first.
4. Verify accessibility and performance with Lighthouse / axe, then update TODO evidence.

Recently completed
- 2026-08-09: 8 posts (zh+en), series 30 -> 38. **B TECHNICAL GROUP COMPLETE
  (17 cells) AND D GROUP COMPLETE (4 cells).** Detail in plan §5.2; older entries archived to
  docs/progress-archive.md. Reusable lessons, ranked by what they cost me:
  (1) TWO AGGREGATES SOLVED SIMULTANEOUSLY IS GUESSING. B12 did that for unit
  prices and got them REVERSED and 5x off; the line-item record was public all
  along. I HAD flagged "that figure looks low" — flagging uncertainty is not
  removing it. D32 applied this: I refused to quantify jammer procurement
  because the pcc API's keyword match is fuzzy, and said so in the post.
  (2) A GAP YOU LIST CAN REFUTE THE POST. B16's own "no control group" line
  killed its first conclusion. Before writing "I did not do X", ask: could X
  refute this? If yes it is prerequisite, not future work. D32 now answers that
  question per gap, in the post itself — make that the standard shape.
  (3) THE RECURRING MISTAKE, 6th and last instance, closed: every cell I had
  filed as needing hardware/factory/interviews/trade secrets needed none.
  (4) To write about a fuzzy word, find where the source is FORCED to enumerate
  it. B13: ModeReason (2nd arg of every set_mode()) = complete autonomy list.
  D32: 內政部's 31-item police-instrument list, and a whole-corpus law search
  (「無人機」19 instruments, 「反制無人機」1) — same move, statutes not code.
  (5) Mis-shaped clause → find its PARENTAGE (B14). (6) Requests-on-record vs
  delivered-text is a reusable check (B14, C24). (7) Negative results only count
  if reproducible — print the query and counts (B13 grep, D32 three law URLs).
  (8) READ THE VERB, not just the actor. D32's whole finding came from noticing
  民航法 §99-13 gives airports 「取締」 while ports/prohibited-airspace get
  「制止或排除」 — I had tabulated that same paragraph in D29 and missed it.
  (9) FACT worth keeping: CCP special-compulsory-measures chapter PASSED
  2024-07-16 (§153-1~§153-10 = GPS / IMSI-catcher / private-space imaging);
  NO article covers aerial evidence. TOOL NOTES archived to progress-archive.
  (10) A BUDGET REVIEW IS AN EVIDENCE SOURCE. D31's six itemised airport
  closures (300 min / 47 flights / 5,929 pax) exist only because the LY Budget
  Center made the CAA produce them. When an agency will not publish a series,
  look for who had power to demand it. Same trick as the Control Yuan report.
  BOOKKEEPING FIXED 2026-08-09: the 未答 column only became standard at B8, so
  20 earlier rows had none — all 38 rows now carry one (B4/B5/B9 copied from the
  posts' own sections, the other 17 marked 回溯補寫). Also corrected two rows
  that still listed gaps the posts had already closed (B12 RS columns, B16
  control group). LESSON (11): an empty gap field reads exactly like "no gaps".
  NEXT BIGGEST HOLES — read plan §三 coverage note, do NOT guess: C22 surveying
  / C25 film / C26 environment / C27 entertainment-industry; all of I except
  61 privacy. Two named, fillable follow-ups I declared in the posts: D31 needs
  post-109/08 closure counts (LY publishes a budget report every year); D30
  needs the 縣市 announced-zone lists and a 函釋 search on 政府機關（構）.
- 2026-08-08 及更早: drone series 0 -> 30 (zh+en). 全部已歸檔到
  docs/progress-archive.md，per-post findings 在 plan §5.2——查覆蓋看那裡。
  仍然約束我的事實與教訓（不要再犯）：
  * THE RECURRING MISTAKE，六次：把題目預設成需要我拿不到的東西（硬體／訪談／
    工廠／商業機密）。六次全部是假的。四條通則在 plan §6。
  * 主張某個移植忠實 → 拿逐字複製的原始碼 diff 它。
  * 系統推得出來的東西不需要旗標；推不出來的才需要別人告訴它。
  * SITE FACTS：`category` 是容器、`drone` tag 才是主題（tags/[tag].astro 無
    category 過濾），每篇必須把 drone 放第一個；未來日期的文章不會被 build，
    已發佈文章連到更晚日期的文章＝硬 404；frontmatter glossary 會 shadow 站
    台預設；沒有 remark-math plugin，數學寫在 code block。
  * 資安檢測規範現行版是 V2.0（2026-04-30）。
  * 英文文章連到中文 slug 曾是全站性缺陷（163 檔／742 連結），修法的三條安全
    規則在 archive 裡，做任何連結批次改動前先讀。

Notes
- Some TODO entries are stale: English home about, Pagefind integration, OG
  image generation, and part of the redesign already exist in the repo.
- agent-* projects (foundation/evidence/flow/artifact/policy/pipelines-unify/
  console/providers/os): code complete, pending production observation windows.
  Status snapshots in docs/progress-archive.md; pending decisions in
  docs/governance/escalation-queue.md.
