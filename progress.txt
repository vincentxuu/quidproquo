# quidproquo progress

Last updated: 2026-08-08

協定（完整版見 docs/governance/operating-charter.md）：
- 這是 working memory，不是日誌。上限 90 行，`pnpm verify` 會擋。
- 任務狀態實質改變就更新本檔（含 Last updated）。
- 完成或過期的條目移到 docs/progress-archive.md，不要無限堆積。
- 需要人類拍板或大模型 session 的事，登錄到 docs/governance/escalation-queue.md。

Current focus
- Close short-term blog infrastructure gaps from `docs/TODO.md`.
- Keep RAG work incremental; feature flags + evaluation required for any addition.
- TW stock screening: four-criteria framework (see tw-stock-screen skill).

In progress
- Align actual implementation status with `docs/TODO.md`.
- Finish low-risk P1 items: RSS author metadata and font preload.

Next steps
1. Backfill missing post `type` fields with a script + sampling/rollback plan.
2. Backfill missing `tldr` fields.
3. Make `type` required in `src/content.config.ts` — escalation queue item, ask first.
4. Verify accessibility and performance with Lighthouse / axe, then update TODO evidence.

Recently completed
- 2026-08-08: drone series 23 -> 24 (zh+en). B8 PX4 vs ArduPilot vs 自研 — the
  first of the NINE-CELL B (technical) block, all of which was empty, and the
  series' first FIRST-HAND output: both stacks cloned, built and one SITL sortie
  flown. THE RECURRING MISTAKE, 4th instance: "technical topic" was silently
  filed under "needs hardware". It is a codebase question. Real barrier was
  Python deps (empy 3.3.4 has no py3.11 wheel), not hardware.
  Findings worth reusing: ArduPilot's AP_NavEKF3.h header cites github.com/PX4/
  ecl for its derivation and Paul Riseborough is a maintainer on BOTH projects —
  the two stacks are common-descent at the hardest layer. Contributor structure
  is INVERTED vs the stereotype: PX4 = company domains (Auterion 380 = 16% of
  human commits), ArduPilot = personal mailboxes but far more concentrated (one
  person 37%); PX4 also has 645 bot commits (22%) that break naive commit
  comparisons. The two real decision points are BSD-3 vs GPLv3 and which layer
  you extend (AP: Lua on the SD card; PX4: ROS 2 outside). Per-post detail in
  plan §5.2 (B8 row) — read that, not this.
  Open: B9-B17 (eight technical cells). Ask FIRST whether each really needs
  hardware or I wrote it to need hardware.
  SITE-WIDE DEFECT found and FIXED (user approved the batch): in-body links in
  English posts pointed at zh slugs, so English readers landed on Chinese posts.
  163 files / 742 links rewritten in two passes (281409f drone series, then the
  rest). Rules used, reuse them: rewrite ONLY when the -en file exists; never
  touch the `🌏 [中文版]` line (361 preserved); category can be NESTED
  (/posts/tech/deep-dive/...) — a single-level regex silently misses 187 links.
  Two zh links left on purpose: ai/2026-07-22-3d-generative-models-landscape has
  no English version. Translating it would close the last gap.
- 2026-08-07: drone series 13 -> 23 (zh+en) — D line, B5 financials, counter-UAS,
  four application posts, privacy. Full entry archived to
  docs/progress-archive.md; per-post findings in plan §5.2.
  THE RECURRING MISTAKE (3x, and see 08-08 for the 4th): treating my own
  pre-writing "素材" estimate as a hard constraint. B2/B4 "needs interviews"
  (false), D1-D3 "needs buying a drone" (false — I had written the topics to
  need it), B4's margin/cash-cycle "needs interviews" (false — filing
  questions). Four-rule 通則 in plan §6; short form: ask which HALF needs
  material, ask whether the topic really needs it or I wrote it to need it, ask
  whether it is truly unavailable or I just haven't looked, and don't treat
  interviews as higher-grade than public records.
  Application/social cells still open: surveying; noise/NIMBY, criminal misuse,
  autonomous-weapons ethics, environmental cost.
- 2026-08-06: drone series — 13 posts shipped (zh+en). Plan + per-post findings
  + verification lessons live in docs/content-plan-drone.md §5.2; read that, not
  this. Decisions that still bind: `category` is the container (folder/URL/nav),
  `drone` tag is the topic — src/pages/tags/[tag].astro has NO category filter,
  so /tags/drone is the cross-category hub and EVERY series post must carry
  `drone` as its first tag. Investing posts: framework-and-risk, no tickers.
  Future-dated posts are NOT built, so a published post linking to a later-dated
  one is a hard 404 (guard script in plan §5.2). Frontmatter glossary is
  prepended before DEFAULT_GLOSSARY_TERMS and deduped first-wins, so it SHADOWS
  the site-wide entry.

Notes
- Some TODO entries are stale: English home about, Pagefind integration, OG
  image generation, and part of the redesign already exist in the repo.
- agent-* projects (foundation/evidence/flow/artifact/policy/pipelines-unify/
  console/providers/os): code complete, pending production observation windows.
  Status snapshots in docs/progress-archive.md; pending decisions in
  docs/governance/escalation-queue.md.
