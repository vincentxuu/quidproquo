# quidproquo progress

Last updated: 2026-08-07

協定（完整版見 docs/governance/operating-charter.md）：
- 這是 working memory，不是日誌。上限 90 行，`pnpm verify` 會擋。
- 任務狀態實質改變就更新本檔（含 Last updated）。
- 完成或過期的條目移到 docs/progress-archive.md，不要無限堆積。
- 需要人類拍板或大模型 session 的事，登錄到 docs/governance/escalation-queue.md。

Current focus
- Close short-term blog infrastructure gaps from `docs/TODO.md`.
- Keep RAG work incremental; feature flags + evaluation required for any addition.
- TW stock screening: four-criteria framework (see tw-stock-screen skill).

In progress
- Align actual implementation status with `docs/TODO.md`.
- Finish low-risk P1 items: RSS author metadata and font preload.

Next steps
1. Backfill missing post `type` fields with a script + sampling/rollback plan.
2. Backfill missing `tldr` fields.
3. Make `type` required in `src/content.config.ts` — escalation queue item, ask first.
4. Verify accessibility and performance with Lighthouse / axe, then update TODO evidence.

Recently completed
- 2026-08-07: drone series — counter-UAS piece (zh+en), the biggest remaining
  gap (series had touched C-UAS three times with no dedicated post). Series now
  18. Technical core: EW "requires a signal to attack" and fiber/satellite/AI
  terminal guidance are removing the signal, pushing defense toward hard kill.
  Taiwan's second layer is the find: Control Yuan report 112國調0010 documents
  MND revising the drone-response SOP THREE TIMES IN TWO MONTHS (2022-07-29
  flare-only, first shot needs the Minister; 2022-08-29 back to 7.62mm
  shoot-down), and the Erdan rock-throwing incident was ruled SOP-compliant.
  Legal basis isn't missing — Civil Aviation Act 99-13 splits enforcement
  across FOUR bodies. Chiang Huang-chih: use law-enforcement power, not
  self-defense (self-defense implies belligerency).
- 2026-08-07: drone series B5 — 「標案毛利多少」用財報回答，不是訪談題。
  Third time the same mistake: I filed margin + cash cycle under "needs an
  interview" when they are filing questions, and I had ALREADY run this method
  on Garuda in A3. Worked example from 雷虎(8033) public filings: GM 35-39%
  (normal for hardware) but opex eats it — operating income negative three
  straight quarters while 2026Q1 net income came entirely from non-operating;
  CCC ≈ 377 days driven by 385 days of INVENTORY (DSO only 45) because tenders
  recognize revenue on acceptance; contract liabilities ~1/10 of quarterly
  revenue = no customer prepayment; capex funded by equity raise, not ops.
  Rule 3 added: "is the material really unavailable, or have I just not looked?"
  Investing-line boundary held: named-company filings used as a worked example,
  zero buy/sell judgment (user approved this reading of the no-tickers rule).
- 2026-08-07: drone series D-line — 3 more posts (zh+en) after dropping the
  "must buy a drone" premise: spec-sheet reading (tech), crash anatomy (tech),
  CAA question-bank analysis (policy). Hub 延伸閱讀
  gained a sixth group. Best finds: the cybersecurity spec defines a product
  "series" as identical flight-control/comms/GNSS modules (regulator, 3晶2軟
  policy and Layer 3 all point at the same three); TTSB's 4-occurrence total is
  gated on ">25kg substantially damaged"; both published reports are the same
  model/maker/agency and both failed in LAYER 2, not the Layer 3 everyone
  discusses. Registered-drone count 75,240→40,134→38,683 is written up as an
  INFERENCE only (Art.10 = 2-year registration validity); do not harden it
  without a CAA statement of the counting basis.
- 2026-08-06: drone series — 13 posts shipped (zh+en each) across tech/policy/
  career/investing/product/education. Plan + per-post findings + cross-post verification lessons
  now live in docs/content-plan-drone.md §5.2 (read that before continuing the
  series; it is the record, this is only working memory).
  Structural decisions that still bind: `category` is the container
  (folder/URL/nav), `drone` tag is the topic — src/pages/tags/[tag].astro has
  NO category filter so /tags/drone is the cross-category hub, and EVERY series
  post must carry `drone` as its first tag. Investing posts go
  framework-and-risk only, no tickers.
  Scheduling mechanics in plan §5.2: future-dated posts are NOT built, so a
  published post linking to a later-dated one is a hard 404; guard script there.
  Glossary debt CLEARED: the 5 drone terms (BVLOS, FPV, 非紅供應鏈, Blue UAS,
  C-UAS) are now in src/lib/glossary/terms.ts (191 total) and the duplicate
  frontmatter block was removed from the hub — note frontmatter glossary is
  prepended before DEFAULT_GLOSSARY_TERMS and deduped first-wins, so a
  frontmatter entry SHADOWS the site-wide one.
  Remaining v1: NOTHING is hard-blocked (see the two 08-07 entries above and
  the four-rule 通則 in plan §6).

Notes
- Some TODO entries are stale: English home about, Pagefind integration, OG
  image generation, and part of the redesign already exist in the repo.
- agent-* projects (foundation/evidence/flow/artifact/policy/pipelines-unify/
  console/providers/os): code complete, pending production observation windows.
  Status snapshots in docs/progress-archive.md; pending decisions in
  docs/governance/escalation-queue.md.
