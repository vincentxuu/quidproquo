---
// src/layouts/PostLayout.astro
import { getLangFromUrl, useTranslations, type Lang } from '../i18n/utils';
import Icons from '../components/Icons.astro';
import { ChatFloating } from '../components/Chat/ChatFloating';

interface Props {
  title: string;
  description?: string;
  lang?: Lang;
  ogImage?: string;
  articleDate?: Date;
  articleTags?: string[];
  switchHref?: string;
  hasTranslation?: boolean;
  hreflangEn?: string;
  hreflangZh?: string;
}
const {
  title,
  description,
  lang: langProp,
  ogImage,
  articleDate,
  articleTags,
  switchHref: switchHrefProp,
  hasTranslation,
  hreflangEn,
  hreflangZh,
} = Astro.props;
const isArticle = !!articleDate;
const lang = langProp ?? getLangFromUrl(Astro.url);
const t = useTranslations(lang);

const homeHref = lang === 'en' ? '/en' : '/';
const categoriesHref = lang === 'en' ? '/en/categories' : '/categories';
const seriesHref = lang === 'en' ? '/en/series' : '/series';
const tagsHref = lang === 'en' ? '/en/tags' : '/tags';
// Post pages pass an explicit switchHref (translation pairing); other pages fall back.
const switchHref = switchHrefProp ?? t('lang.switch.href');
const switchLabel = t('lang.switch');
// Emit hreflang alternates only when a translation pair exists. x-default -> zh-TW.
const showHreflang = hasTranslation && !!hreflangEn && !!hreflangZh;
const canonicalURL = new URL(Astro.url.pathname, Astro.site ?? 'https://quidproquo.cc');
---
<!DOCTYPE html>
<html lang={lang}>
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>{title} — quidproquo</title>
  <link rel="canonical" href={canonicalURL} />
  {description && <meta name="description" content={description} />}
  <meta property="og:title" content={`${title} — quidproquo`} />
  {description && <meta property="og:description" content={description} />}
  <meta property="og:locale" content={lang === 'en' ? 'en_US' : 'zh_TW'} />
  <meta property="og:type" content={isArticle ? 'article' : 'website'} />
  <meta property="og:url" content={canonicalURL} />
  {ogImage && <meta property="og:image" content={ogImage} />}
  {ogImage && <meta property="og:image:width" content="1200" />}
  {ogImage && <meta property="og:image:height" content="630" />}
  {isArticle && <meta property="article:published_time" content={articleDate!.toISOString()} />}
  {isArticle && <meta property="article:author" content="xiaoxu" />}
  {isArticle && articleTags?.map(tag => <meta property="article:tag" content={tag} />)}
  <meta name="twitter:card" content={ogImage ? 'summary_large_image' : 'summary'} />
  <meta name="twitter:title" content={`${title} — quidproquo`} />
  {description && <meta name="twitter:description" content={description} />}
  {ogImage && <meta name="twitter:image" content={ogImage} />}
  <link rel="alternate" type="application/rss+xml" title="quidproquo RSS" href={lang === 'en' ? '/en/rss.xml' : '/rss.xml'} />
  {showHreflang && <link rel="alternate" hreflang="en" href={hreflangEn} />}
  {showHreflang && <link rel="alternate" hreflang="zh-Hant" href={hreflangZh} />}
  {showHreflang && <link rel="alternate" hreflang="x-default" href={hreflangZh} />}
  <link rel="preload" href="/fonts/NotoSansTC-Medium.otf" as="font" type="font/otf" crossorigin />
  <slot name="head" />
  <script is:inline>
    // Apply theme before page renders to avoid flash
    const stored = localStorage.getItem('theme');
    const prefersDark = window.matchMedia('(prefers-color-scheme: dark)').matches;
    if (stored === 'dark' || (!stored && prefersDark)) {
      document.documentElement.setAttribute('data-theme', 'dark');
    }
  </script>
</head>
<body>
  <div class="progress-bar" id="progress-bar"></div>
  <a href="#main-content" class="skip-nav">Skip to content</a>
  <header>
    <nav>
      <a href={homeHref} class="logo">quidproquo</a>
      <a href={lang === 'en' ? '/en/about' : '/about'} class="nav-link">{t('nav.about')}</a>
      <a href={categoriesHref} class="nav-link">{t('nav.categories')}</a>
      <a href={seriesHref} class="nav-link">{t('nav.series')}</a>
      <a href={tagsHref} class="nav-link">{t('nav.tags')}</a>
      <div class="nav-right">
        <a href={lang === 'en' ? '/en/search' : '/search'} class="nav-search-link" aria-label={t('nav.search')}>
          <Icons name="search" size={15} />
        </a>
        <button class="nav-icon-btn" id="theme-toggle" aria-label={t('nav.toggle-theme')}>
          <span class="icon-light"><Icons name="sun" size={15} /></span>
          <span class="icon-dark"><Icons name="moon" size={15} /></span>
        </button>
        <a href={switchHref} class="lang-badge">{switchLabel}</a>
      </div>
    </nav>
  </header>
  <main id="main-content">
    <slot />
  </main>
  <footer>
    <span>quidproquo.cc</span>
    <a href={lang === 'en' ? '/en/rss.xml' : '/rss.xml'} class="rss-link" aria-label="RSS">
      <Icons name="rss" size={14} />
      <span>RSS</span>
    </a>
  </footer>

  <ChatFloating client:only="react" />

  <!-- Floating scroll-to-top -->
  <button class="scroll-top" id="scroll-top" aria-label="Scroll to top">
    <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><polyline points="18 15 12 9 6 15"/></svg>
    <span class="scroll-pct" id="scroll-pct">0%</span>
  </button>
</body>
</html>

<style is:global>
  @font-face {
    font-family: 'Noto Sans TC Local';
    src: url('/fonts/NotoSansTC-Medium.otf') format('opentype');
    font-weight: 500;
    font-style: normal;
    font-display: swap;
  }

  /* ── Dark Navy Design Tokens (inspired by DJ MYG) ── */
  :root {
    --brand-900: #1a2e1a;
    --brand-700: #2d4a2d;
    --brand-500: #4a9e6a;
    --brand-300: #a7c4a0;
    --brand-100: #e8f5e8;
    --brand-50:  #f0f7f0;
    --border:    #d1e8d1;
    --text-primary:    #1a1a1a;
    --text-secondary:  #4a7c59;
    --text-muted:      #767676;
    --bg-page:   #ffffff;
    --bg-subtle: #f5f7f5;
    --bg-card:   #ffffff;
    --bg-code:   #1e1e2e;
    --heading-accent: #e8657a;
    --info-bg:    #eef6ff;
    --info-border: #3b82f6;
    --info-icon:   #3b82f6;
    --success-bg:    #eefbf3;
    --success-border: #22c55e;
    --success-icon:   #22c55e;
    --warning-bg:    #fffbeb;
    --warning-border: #f59e0b;
    --shadow-card-hover: 0 2px 12px rgba(0, 0, 0, 0.08);
    --shadow-floating: 0 2px 12px rgba(0, 0, 0, 0.15);
    --code-dot-red: #ff5f57;
    --code-dot-yellow: #febc2e;
    --code-dot-green: #28c840;

    /* Category badge colors */
    --cat-tech:      #1a1a1a;
    --cat-ai:        #6d28d9;
    --cat-product:   #0369a1;
    --cat-education: #b45309;
    --cat-life:      #15803d;
    --cat-marketing: #be185d;
    --cat-travel:    #c2410c;
    --cat-climbing:  #57534e;
    --cat-surf:      #0891b2;
    --cat-film:      #292524;
    --cat-coffee:    #92400e;
    --cat-learning:  #0d9488;
    --cat-design:    #a21caf;
    --cat-policy:    #475569;
    --cat-anime:     #e11d48;
    --cat-career:    #0c4a6e;
  }

  [data-theme="dark"] {
    --brand-900: #e8e8e8;
    --brand-700: #c0c0c0;
    --brand-500: #5cb87a;
    --brand-300: #2d4a2d;
    --brand-100: #1c2833;
    --brand-50:  #151d26;
    --border:    #2a3545;
    --text-primary:    #d1d5db;
    --text-secondary:  #5cb87a;
    --text-muted:      #6b7280;
    --bg-page:   #0f1419;
    --bg-subtle: #151d26;
    --bg-card:   #1a2332;
    --bg-code:   #0d1117;
    --heading-accent: #f472b6;
    --info-bg:    #151d26;
    --info-border: #3b82f6;
    --info-icon:   #60a5fa;
    --success-bg:    #151d26;
    --success-border: #22c55e;
    --success-icon:   #4ade80;
    --warning-bg:    #1a1a0f;
    --warning-border: #f59e0b;
    --shadow-card-hover: 0 2px 12px rgba(0, 0, 0, 0.25);
    --shadow-floating: 0 2px 12px rgba(0, 0, 0, 0.4);
  }

  /* Skip navigation */
  .skip-nav {
    position: absolute;
    left: -9999px;
    top: 1rem;
    z-index: 9999;
    background: var(--brand-900);
    color: var(--bg-page);
    padding: 0.5rem 1rem;
    border-radius: 4px;
    font-size: 0.875rem;
    text-decoration: none;
  }
  .skip-nav:focus {
    left: 1rem;
  }

  /* Keyboard focus styles */
  :focus-visible {
    outline: 2px solid var(--brand-500);
    outline-offset: 2px;
  }

  /* ── Reset & Base ── */
  *, *::before, *::after { box-sizing: border-box; }

  html {
    overflow-x: hidden; /* prevent wide tables/code from expanding page width */
  }

  body {
    font-family: 'Noto Sans TC Local', 'PingFang TC', 'Microsoft JhengHei', system-ui, -apple-system, sans-serif;
    max-width: 800px;
    margin: 0 auto;
    padding: 1rem 1.5rem;
    color: var(--text-primary);
    background: var(--bg-page);
    transition: background 0.2s, color 0.2s;
  }

  /* ── Nav ── */
  nav {
    display: flex;
    align-items: center;
    gap: 1.5rem;
    margin-bottom: 2rem;
    padding-bottom: 1rem;
    border-bottom: 2px solid var(--border);
    position: relative;
  }
  .logo {
    font-weight: 900;
    font-size: 1.05rem;
    letter-spacing: -0.5px;
    text-decoration: none;
    color: var(--brand-900);
  }
  .nav-link {
    font-size: 0.8rem;
    text-decoration: none;
    color: var(--text-secondary);
  }
  .nav-link:hover { color: var(--brand-900); }
  .nav-right {
    margin-left: auto;
    display: flex;
    align-items: center;
    gap: 0.6rem;
  }

  /* Circular icon buttons (like DJ MYG) */
  .nav-icon-btn {
    width: 36px;
    height: 36px;
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    background: var(--bg-subtle);
    border: 1px solid var(--border);
    color: var(--text-muted);
    cursor: pointer;
    text-decoration: none;
    transition: color 0.15s, border-color 0.15s, background 0.15s;
  }
  .nav-icon-btn:hover {
    color: var(--brand-500);
    border-color: var(--brand-500);
    background: var(--bg-card);
  }
  .nav-search-link {
    min-height: 36px;
    padding: 0 0.8rem;
    border-radius: 999px;
    display: inline-flex;
    align-items: center;
    justify-content: center;
    gap: 0.45rem;
    background: var(--bg-subtle);
    border: 1px solid var(--border);
    color: var(--text-secondary);
    cursor: pointer;
    text-decoration: none;
    font-size: 0.78rem;
    font-weight: 700;
    letter-spacing: 0.02em;
    transition: color 0.15s, border-color 0.15s, background 0.15s;
  }
  .nav-search-link:hover {
    color: var(--brand-500);
    border-color: var(--brand-500);
    background: var(--bg-card);
  }

  /* Dark mode toggle */
  .icon-dark { display: none; }
  [data-theme="dark"] .icon-light { display: none; }
  [data-theme="dark"] .icon-dark  { display: flex; }

  /* Language badge */
  .lang-badge {
    font-size: 0.7rem;
    font-weight: 700;
    letter-spacing: 0.06em;
    color: var(--text-secondary);
    border: 1px solid var(--border);
    padding: 0.12rem 0.45rem;
    border-radius: 3px;
    text-decoration: none;
  }
  .lang-badge:hover { border-color: var(--brand-500); }

  /* Reading progress bar */
  .progress-bar {
    position: fixed;
    top: 0;
    left: 0;
    width: 0%;
    height: 2px;
    background: var(--brand-500);
    transition: width 0.1s linear;
    z-index: 1000;
  }

  /* ── Floating scroll-to-top ── */
  .scroll-top {
    position: fixed;
    bottom: 5.5rem;
    right: 1.5rem;
    width: 48px;
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 2px;
    background: var(--bg-card);
    border: 1px solid var(--border);
    border-radius: 8px;
    padding: 0.5rem 0.35rem;
    color: var(--heading-accent);
    cursor: pointer;
    opacity: 0;
    transform: translateY(10px);
    transition: opacity 0.2s, transform 0.2s, color 0.15s;
    pointer-events: none;
    z-index: 100;
    box-shadow: var(--shadow-floating);
  }
  .scroll-top.visible {
    opacity: 1;
    transform: translateY(0);
    pointer-events: auto;
  }
  .scroll-top:hover { color: var(--brand-500); }
  .scroll-pct {
    font-size: 0.6rem;
    font-weight: 700;
    line-height: 1;
  }

  /* ── Footer ── */
  footer {
    margin-top: 3rem;
    padding-top: 1rem;
    border-top: 1px solid var(--border);
    display: flex;
    align-items: center;
    justify-content: space-between;
    font-size: 0.82rem;
    color: var(--text-muted);
  }
  .rss-link {
    display: flex;
    align-items: center;
    gap: 0.4rem;
    color: var(--text-muted);
    text-decoration: none;
    font-size: 0.78rem;
  }
  .rss-link:hover { color: var(--brand-500); }

  /* ── Category badges (global, used in PostCard + slug page) ── */
  .cat-badge {
    font-size: 0.62rem;
    font-weight: 700;
    padding: 0.12rem 0.45rem;
    border-radius: 2px;
    text-transform: uppercase;
    letter-spacing: 0.04em;
    color: #fff;
    text-decoration: none;
  }
  .cat-tech      { background: var(--cat-tech); }
  .cat-ai        { background: var(--cat-ai); }
  .cat-product   { background: var(--cat-product); }
  .cat-education { background: var(--cat-education); }
  .cat-life      { background: var(--cat-life); }
  .cat-marketing { background: var(--cat-marketing); }
  .cat-travel    { background: var(--cat-travel); }
  .cat-climbing  { background: var(--cat-climbing); }
  .cat-surf      { background: var(--cat-surf); }
  .cat-film      { background: var(--cat-film); }
  .cat-coffee    { background: var(--cat-coffee); }
  .cat-learning  { background: var(--cat-learning); }
  .cat-design    { background: var(--cat-design); }
  .cat-policy    { background: var(--cat-policy); }
  .cat-anime     { background: var(--cat-anime); }
  .cat-career    { background: var(--cat-career); }

  /* ── Type badges ── */
  .type-badge {
    font-size: 0.62rem;
    font-weight: 700;
    padding: 0.1rem 0.4rem;
    border-radius: 2px;
    text-transform: uppercase;
    letter-spacing: 0.04em;
    border: 1px solid currentColor;
    text-decoration: none;
  }
  .type-badge:hover { opacity: 0.75; }
  .type-debug     { color: #d97706; }
  .type-deep-dive { color: #4f46e5; }
  .type-guide     { color: #0d9488; }
  .type-project   { color: #64748b; }

  /* ── Type filter tabs ── */
  .type-filter {
    display: flex;
    gap: 0.35rem;
    flex-wrap: wrap;
    margin-bottom: 1.5rem;
  }
  .filter-btn {
    background: none;
    border: 1px solid var(--border);
    border-radius: 3px;
    padding: 0.25rem 0.65rem;
    font-size: 0.72rem;
    font-weight: 700;
    cursor: pointer;
    color: var(--text-secondary);
    text-transform: uppercase;
    letter-spacing: 0.04em;
    transition: border-color 0.15s, color 0.15s, background 0.15s;
  }
  .filter-btn:hover { border-color: var(--brand-500); color: var(--brand-500); }
  .filter-btn.active { background: var(--brand-900); border-color: var(--brand-900); color: var(--bg-page); }

  /* ── Tags ── */
  .tag-pill {
    background: var(--brand-100);
    font-size: 0.72rem;
    padding: 0.08rem 0.45rem;
    border-radius: 3px;
    color: var(--brand-700);
    text-decoration: none;
  }
  .tag-pill:hover { background: var(--brand-300); }

  /* ── Prose: all MDX/markdown rendered content ── */
  .content { line-height: 1.8; margin-bottom: 3rem; }

  /* Headings — with colored underline accent (like DJ MYG) */
  .content h1 { font-size: 1.7rem; font-weight: 900; margin: 2.5rem 0 1rem; color: var(--brand-900); }
  .content h2 {
    font-size: 1.3rem;
    font-weight: 800;
    margin: 2.5rem 0 0.75rem;
    padding-bottom: 0.5rem;
    border-bottom: none;
    color: var(--brand-900);
    position: relative;
    display: flex;
    align-items: baseline;
    justify-content: space-between;
  }
  .content h2::after {
    content: '';
    position: absolute;
    bottom: 0;
    left: 0;
    width: 3rem;
    height: 3px;
    background: var(--heading-accent);
    border-radius: 2px;
  }
  .content h3 { font-size: 1.05rem; font-weight: 700; margin: 1.5rem 0 0.5rem; color: var(--brand-900); }
  .content h4 { font-size: 0.95rem; font-weight: 700; margin: 1.25rem 0 0.4rem; color: var(--brand-900); }
  .content h5, .content h6 { font-size: 0.9rem; font-weight: 600; margin: 1rem 0 0.35rem; color: var(--text-secondary); }

  /* Heading anchor links */
  .content h2 .heading-anchor,
  .content h3 .heading-anchor {
    color: var(--text-muted);
    text-decoration: none;
    font-weight: 400;
    font-size: 0.85em;
    opacity: 0;
    transition: opacity 0.15s;
    margin-left: 0.5rem;
  }
  .content h2:hover .heading-anchor,
  .content h3:hover .heading-anchor { opacity: 1; }

  /* Paragraph & inline */
  .content p { margin-bottom: 1rem; }
  .content strong { font-weight: 700; color: var(--text-primary); }
  .content em { font-style: italic; }
  .content del { text-decoration: line-through; color: var(--text-muted); }
  .content mark { background: #fef08a; color: #1a1a1a; padding: 0.05em 0.2em; border-radius: 2px; }

  /* Links */
  .content a { color: var(--brand-500); text-decoration: underline; text-decoration-color: var(--brand-300); text-underline-offset: 2px; }
  .content a:hover { color: var(--brand-900); text-decoration-color: var(--brand-500); }

  /* ── macOS-style Code Blocks (like DJ MYG) ── */
  .content pre {
    position: relative;
    background: var(--bg-code);
    color: #d4d4d4;
    padding: 0;
    border-radius: 8px;
    overflow: hidden;
    margin-bottom: 1.25rem;
    border: 1px solid var(--border);
  }
  .content pre::before {
    content: '';
    display: block;
    padding: 0.6rem 1rem;
    background: rgba(0,0,0,0.15);
    background-image: radial-gradient(circle at 16px 50%, var(--code-dot-red) 5px, transparent 5px),
                      radial-gradient(circle at 36px 50%, var(--code-dot-yellow) 5px, transparent 5px),
                      radial-gradient(circle at 56px 50%, var(--code-dot-green) 5px, transparent 5px);
    background-repeat: no-repeat;
    border-bottom: 1px solid rgba(255,255,255,0.05);
  }
  .content pre code {
    display: block;
    padding: 1rem 1.5rem;
    overflow-x: auto;
    font-size: 0.88rem;
    line-height: 1.6;
    counter-reset: line;
  }

  .content :not(pre) > code { background: var(--brand-100); color: var(--brand-700); padding: 0.1em 0.35em; border-radius: 3px; font-size: 0.9em; }
  .content kbd { background: var(--bg-subtle); border: 1px solid var(--border); border-radius: 3px; padding: 0.1em 0.4em; font-size: 0.85em; font-family: monospace; }

  /* ── Callout / Blockquote boxes (like DJ MYG info/success/warning) ── */
  .content blockquote {
    background: var(--info-bg);
    border-left: 4px solid var(--info-border);
    border-radius: 0 8px 8px 0;
    padding: 1rem 1.25rem;
    color: var(--text-primary);
    margin: 1.25rem 0;
    font-style: normal;
  }
  .content blockquote p { margin-bottom: 0.35rem; }
  .content blockquote p:last-child { margin-bottom: 0; }

  /* Tip callout (blockquote starting with [!TIP] or > **tip**) */
  .content blockquote.callout-success,
  .content blockquote:has(> p:first-child > strong:first-child:is([data-type="success"])) {
    background: var(--success-bg);
    border-left-color: var(--success-border);
  }

  /* Lists */
  .content ul, .content ol { padding-left: 1.5rem; margin-bottom: 1rem; }
  .content ul { list-style-type: disc; }
  .content ol { list-style-type: decimal; }
  .content li { margin-bottom: 0.35rem; }
  .content li > ul, .content li > ol { margin-top: 0.25rem; margin-bottom: 0.25rem; }
  .content input[type="checkbox"] { margin-right: 0.4rem; accent-color: var(--brand-500); }

  /* Horizontal rule — dashed style (like DJ MYG) */
  .content hr {
    border: none;
    border-top: 2px dashed var(--border);
    margin: 2.5rem 0;
  }

  /* Images */
  .content img { max-width: 100%; height: auto; border-radius: 8px; margin: 1rem 0; display: block; }
  .content figure { margin: 1.5rem 0; }
  .content figcaption { font-size: 0.8rem; color: var(--text-muted); text-align: center; margin-top: 0.4rem; }

  /* ── Responsive: mobile ── */
  @media (max-width: 640px) {
    body { padding-left: 1rem; padding-right: 1rem; }
    nav {
      flex-wrap: wrap;
      gap: 0.5rem 1rem;
      row-gap: 0.75rem;
    }
    .nav-link {
      font-size: 0.75rem;
    }
    .nav-right {
      gap: 0.5rem;
    }
    .nav-icon-btn,
    .nav-search-link {
      min-height: 32px;
      height: 32px;
      width: 32px;
      padding: 0 0.6rem;
    }
    .lang-badge {
      font-size: 0.65rem;
      padding: 0.1rem 0.4rem;
    }
  }
  @media (max-width: 400px) {
    body { padding-left: 0.75rem; padding-right: 0.75rem; }
    nav {
      gap: 0.4rem 0.75rem;
      row-gap: 0.6rem;
    }
    .logo {
      font-size: 0.95rem;
    }
    .nav-link {
      font-size: 0.7rem;
    }
    .nav-icon-btn,
    .nav-search-link {
      min-height: 30px;
      height: 30px;
      width: 30px;
      padding: 0 0.5rem;
    }
  }

  /* ── Dark styled Tables (like DJ MYG) ── */
  .content .table-scroll {
    overflow-x: auto;
    -webkit-overflow-scrolling: touch;
    border: 1px solid var(--border);
    border-radius: 8px;
    margin-bottom: 1.5rem;
  }
  .content table {
    width: max-content;
    min-width: 100%;
    border-collapse: separate;
    border-spacing: 0;
    font-size: 0.9rem;
    border-radius: 8px;
  }
  .content th {
    background: var(--bg-subtle);
    font-weight: 700;
    text-align: left;
    padding: 0.7rem 1rem;
    border-bottom: 2px solid var(--border);
    color: var(--text-primary);
  }
  .content td {
    padding: 0.65rem 1rem;
    border-bottom: 1px solid var(--border);
    color: var(--text-primary);
  }
  .content tr:last-child td { border-bottom: none; }
  .content tbody tr:hover { background: var(--bg-subtle); }
  .content th:not(:last-child),
  .content td:not(:last-child) {
    border-right: 1px solid var(--border);
  }

  /* ── About page: two-column layout ── */
  .about-grid {
    display: grid;
    grid-template-columns: 180px 1fr;
    gap: 0;
    align-items: start;
  }

  .about-sidebar {
    position: sticky;
    top: 5.5rem;
    align-self: start;
  }

  .about-sidebar-inner {
    padding-right: 2rem;
    border-right: 1px solid var(--border);
  }

  .about-sidebar-name {
    font-family: 'DM Serif Display', Georgia, serif;
    font-style: italic;
    font-size: 2rem;
    line-height: 1.05;
    color: var(--brand-900);
    margin: 0 0 0.4rem;
    letter-spacing: -0.01em;
  }

  .about-sidebar-title {
    font-size: 0.68rem;
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 0.09em;
    color: var(--text-muted);
    margin: 0 0 1rem;
  }

  .about-sidebar-blurb {
    margin: 0 0 1rem;
    font-size: 0.84rem;
    line-height: 1.7;
    color: var(--text-secondary);
    max-width: 16rem;
  }

  .about-sidebar-rule {
    height: 1px;
    background: var(--border);
    margin: 0.9rem 0;
  }

  .about-sidebar-nav {
    display: flex;
    flex-direction: column;
    gap: 0.4rem;
  }

  .about-sidebar-nav a {
    display: inline-flex;
    align-items: center;
    gap: 0.42rem;
    font-size: 0.78rem;
    color: var(--text-secondary);
    text-decoration: none;
    transition: color 0.12s;
  }

  .about-sidebar-nav a svg {
    width: 0.9rem;
    height: 0.9rem;
    flex: 0 0 auto;
    fill: currentColor;
  }

  .about-sidebar-nav a:hover {
    color: var(--brand-500);
    text-decoration: none;
  }

  .about-body {
    padding-left: 2.5rem;
    padding-right: 1rem;
    padding-bottom: 3rem;
    line-height: 1.85;
    max-width: 40rem;
  }

  .about-body p {
    margin-bottom: 1.4rem;
    line-height: 1.85;
    color: var(--text-primary);
  }

  .about-prose {
    width: 100%;
  }

  .content .about-h1 {
    font-family: 'DM Serif Display', Georgia, serif;
    font-size: 2.4rem;
    font-weight: 400;
    letter-spacing: -0.02em;
    color: var(--brand-900);
    margin: 0 0 1.4rem;
    line-height: 1.1;
  }

  .about-section-h2 {
    font-family: 'DM Serif Display', Georgia, serif !important;
    font-size: 1.4rem !important;
    font-weight: 400 !important;
    color: var(--brand-900) !important;
    margin: 2.5rem 0 1rem !important;
    padding-bottom: 0.35rem !important;
    border-bottom: 1px solid var(--border) !important;
    line-height: 1.2 !important;
    display: block !important;
    justify-content: unset !important;
  }

  .about-section-h2::after { display: none !important; }

  .about-intro-lede {
    font-size: 1.02rem;
    line-height: 1.85;
    color: var(--text-primary);
    border-left: 3px solid var(--brand-500);
    padding-left: 1.1rem;
    margin-bottom: 1.2rem;
  }

  .about-notes {
    display: flex;
    flex-direction: column;
    gap: 1rem;
    margin: 1.1rem 0 1.2rem;
  }

  .about-principles {
    display: flex;
    flex-direction: column;
    gap: 0.7rem;
    padding: 1rem 1.05rem;
    background: var(--bg-subtle);
    border: 1px solid var(--border);
    border-radius: 10px;
  }

  .about-principles p {
    margin: 0;
    font-size: 0.92rem;
    line-height: 1.7;
    color: var(--text-secondary);
  }

  .about-principles strong {
    color: var(--brand-500);
    font-weight: 800;
  }

  .about-principles em {
    font-style: italic;
    color: var(--brand-500);
  }

  .about-offwork {
    display: flex;
    flex-direction: column;
    gap: 0.75rem;
  }

  .about-offwork-tags {
    display: flex;
    flex-wrap: wrap;
    gap: 0.45rem;
  }

  .about-offwork-tag {
    display: inline-flex;
    align-items: center;
    min-height: 1.9rem;
    padding: 0.22rem 0.62rem;
    border-radius: 999px;
    background: var(--bg-subtle);
    border: 1px solid var(--border);
    font-size: 0.74rem;
    font-weight: 700;
    letter-spacing: 0.03em;
    color: var(--text-secondary);
  }

  .about-offwork p {
    margin: 0;
    font-size: 0.92rem;
    line-height: 1.8;
    color: var(--text-primary);
  }

  .about-timeline {
    position: relative;
    display: flex;
    flex-direction: column;
    gap: 1.05rem;
    margin: 1.35rem 0 1.5rem;
  }

  .about-timeline::before {
    content: "";
    position: absolute;
    left: 4.75rem;
    top: 0.35rem;
    bottom: 0.35rem;
    width: 1px;
    background: linear-gradient(180deg, var(--brand-300) 0%, var(--border) 100%);
  }

  .about-timeline-item {
    position: relative;
    display: grid;
    grid-template-columns: 4.2rem 1fr;
    gap: 0.9rem;
    align-items: start;
  }

  .about-timeline-item:not(:last-child) {
    margin-bottom: 0.35rem;
  }

  .about-timeline-item::before {
    content: "";
    position: absolute;
    left: 4.35rem;
    top: 0.75rem;
    width: 0.7rem;
    height: 0.7rem;
    border-radius: 999px;
    background: var(--brand-500);
    border: 3px solid var(--bg-card);
    box-shadow: 0 0 0 1px var(--brand-300);
  }

  .about-timeline-year {
    font-size: 0.68rem;
    font-weight: 800;
    letter-spacing: 0.08em;
    text-transform: uppercase;
    color: var(--text-muted);
    padding-top: 0.45rem;
    margin-left: 0.1rem;
  }

  .about-timeline-content {
    background: var(--bg-subtle);
    border: 1px solid var(--border);
    border-radius: 12px;
    margin-left: 0.5rem;
    padding: 0.85rem 1rem 0.9rem;
  }

  .about-timeline-content h3 {
    margin: 0 0 0.3rem;
    font-size: 0.98rem;
    font-weight: 800;
    color: var(--brand-900);
    line-height: 1.35;
  }

  .about-timeline-content p {
    margin: 0;
    font-size: 0.92rem;
    line-height: 1.75;
    color: var(--text-secondary);
  }

  .about-now-label {
    display: inline-flex;
    align-items: center;
    gap: 0.55rem;
  }

  .about-now-list {
    list-style: none !important;
    padding: 0 !important;
    margin: 0 0 1.4rem !important;
    display: flex;
    flex-direction: column;
    gap: 0.4rem;
  }

  .about-now-list li {
    display: flex;
    align-items: baseline;
    gap: 0.65rem;
    padding: 0.55rem 0.85rem;
    background: var(--bg-subtle);
    border: 1px solid var(--border);
    border-radius: 6px;
    font-size: 0.9rem;
    line-height: 1.6;
    color: var(--text-secondary);
    transition: border-color 0.15s;
    margin-bottom: 0 !important;
  }

  .about-now-list li::before {
    content: "→";
    color: var(--brand-500);
    font-weight: 700;
    flex-shrink: 0;
  }

  .about-now-list li:hover { border-color: var(--brand-500); }

  .about-projects {
    display: flex;
    flex-direction: column;
    gap: 0.75rem;
    margin-bottom: 0.5rem;
  }

  .about-page .project-card {
    padding: 1rem 1.15rem;
    background: var(--bg-subtle);
    border: 1px solid var(--border);
    border-left: 3px solid var(--brand-500);
    border-radius: 0 8px 8px 0;
    transition: border-left-color 0.15s, box-shadow 0.15s, transform 0.15s;
  }

  .about-page .project-card:hover {
    border-left-color: var(--heading-accent);
    box-shadow: var(--shadow-card-hover);
    transform: translateX(3px);
  }

  .about-page .project-header {
    display: flex;
    align-items: baseline;
    gap: 0.55rem;
    margin-bottom: 0.35rem;
  }

  .about-page .project-name {
    font-size: 0.96rem;
    font-weight: 800;
    color: var(--brand-900);
  }

  .about-page .project-badge {
    font-size: 0.59rem;
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 0.06em;
    color: var(--text-muted);
    border: 1px solid var(--border);
    padding: 0.1rem 0.35rem;
    border-radius: 2px;
  }

  .about-page .project-desc {
    font-size: 0.85rem;
    color: var(--text-secondary);
    line-height: 1.65;
    margin: 0 0 0.55rem;
  }

  .about-page .project-tags {
    display: flex;
    flex-wrap: wrap;
    gap: 0.28rem;
    margin-bottom: 0.55rem;
  }

  .about-page .project-links { display: flex; gap: 0.85rem; }

  .about-page .project-link {
    font-size: 0.77rem;
    font-weight: 600;
    color: var(--brand-500);
    text-decoration: none;
  }

  .about-page .project-link:hover { color: var(--brand-900); text-decoration: none; }

  .about-page .ptag {
    font-size: 0.62rem;
    font-weight: 600;
    background: var(--brand-100);
    color: var(--brand-700);
    padding: 0.1rem 0.42rem;
    border-radius: 3px;
  }

  .about-skills {
    display: flex;
    flex-direction: column;
    gap: 0.65rem;
    margin-bottom: 0.5rem;
  }

  .about-page .skill-row {
    display: flex;
    align-items: flex-start;
    gap: 1rem;
  }

  .about-page .skill-cat {
    font-size: 0.65rem;
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 0.07em;
    color: var(--text-muted);
    min-width: 88px;
    padding-top: 0.18rem;
    flex-shrink: 0;
  }

  .about-page .skill-tags { display: flex; flex-wrap: wrap; gap: 0.3rem; }

  .about-links { display: flex; flex-wrap: wrap; gap: 0.55rem; }

  .about-page .link-chip {
    display: inline-flex;
    align-items: center;
    gap: 0.4rem;
    font-size: 0.82rem;
    font-weight: 600;
    color: var(--text-secondary);
    border: 1px solid var(--border);
    padding: 0.36rem 0.82rem;
    border-radius: 6px;
    text-decoration: none;
    background: var(--bg-subtle);
    transition: color 0.15s, border-color 0.15s;
  }

  .about-page .link-chip:hover {
    color: var(--brand-500);
    border-color: var(--brand-500);
    text-decoration: none;
  }

  @media (max-width: 680px) {
    .about-page {
      margin-top: 0.4rem;
      padding-left: 0.75rem;
      padding-right: 0.75rem;
    }
    .about-grid {
      grid-template-columns: 1fr;
      gap: 0.9rem;
    }
    .about-sidebar { position: static; }
    .about-page .about-sidebar-inner {
      border-right: none;
      border-bottom: 1px solid var(--border);
      padding-right: 0;
      padding-bottom: 0;
      margin-bottom: 0;
      display: block !important;
      width: 100%;
      background: var(--brand-50);
      border: 1px solid var(--border);
      border-radius: 14px;
      padding: 1.1rem 1.25rem 1rem;
    }
    .about-page .about-sidebar-name { font-size: 1.4rem !important; margin-bottom: 0.15rem; }
    .about-page .about-sidebar-title { font-size: 0.62rem; margin-bottom: 0.7rem; }
    .about-page .about-sidebar-blurb {
      display: block;
      margin: 0 0 0.75rem;
      font-size: 0.86rem;
      line-height: 1.65;
      max-width: 18rem;
    }
    .about-page .about-sidebar-rule { display: none; }
    .about-page .about-sidebar-nav {
      display: flex !important;
      width: 100%;
      flex-direction: row;
      flex-wrap: wrap;
      gap: 0.45rem;
      margin-top: 0;
    }
    .about-page .about-sidebar-nav a {
      display: inline-flex;
      align-items: center;
      min-height: 2rem;
      padding: 0.24rem 0.58rem;
      border: 1px solid var(--border);
      border-radius: 999px;
      background: var(--bg-card);
      font-size: 0.72rem;
      line-height: 1;
    }
    .about-body {
      --about-mobile-inset-left: 1.05rem;
      --about-mobile-inset-right: 0.45rem;
      padding: 1.25rem 1.5rem 1.7rem;
      background: linear-gradient(180deg, var(--bg-card) 0%, var(--bg-subtle) 100%);
      border: 1px solid var(--border);
      border-radius: 16px;
    }
    .content .about-h1 {
      font-size: 2.05rem;
      margin-bottom: 1.15rem;
      padding-left: var(--about-mobile-inset-left);
      padding-right: var(--about-mobile-inset-right);
    }
    .about-prose {
      max-width: 30rem;
      margin-left: auto;
      margin-right: auto;
      padding-left: var(--about-mobile-inset-left);
      padding-right: var(--about-mobile-inset-right);
    }
    .about-body p {
      font-size: 1rem;
      line-height: 2;
      margin-top: 0.25rem;
      margin-bottom: 1.15rem;
      text-wrap: pretty;
    }
    .about-timeline {
      gap: 1.1rem;
      margin-top: 1.2rem;
    }
    .about-timeline::before {
      left: 0.6rem;
      top: 0.25rem;
      bottom: 0.25rem;
    }
    .about-timeline-item {
      grid-template-columns: 1fr;
      gap: 0.4rem;
      padding-left: 2.45rem;
    }

    .about-timeline-item:not(:last-child) {
      margin-bottom: 0.9rem;
    }
    .about-timeline-item::before {
      left: 0.18rem;
      top: 0.2rem;
      width: 0.65rem;
      height: 0.65rem;
    }
    .about-timeline-year {
      padding-top: 0.02rem;
      font-size: 0.7rem;
      letter-spacing: 0.09em;
      margin-left: 0.45rem;
    }
    .about-timeline-content {
      margin-left: 0.8rem;
      padding: 0.85rem 0.95rem 0.9rem;
      border-radius: 14px;
    }
    .about-timeline-content h3 {
      font-size: 0.95rem;
      margin-bottom: 0.35rem;
    }
    .about-timeline-content p {
      font-size: 0.9rem;
      line-height: 1.72;
    }
    .about-intro-lede {
      font-size: 1rem !important;
      padding-left: 0.95rem;
      margin-bottom: 1.35rem;
    }
    .about-notes {
      gap: 0.9rem;
      margin: 1.05rem 0 1.2rem;
    }
    .about-principles {
      padding: 0.9rem 0.95rem;
      border-radius: 12px;
      gap: 0.65rem;
    }
    .about-principles p {
      font-size: 0.88rem;
      line-height: 1.68;
    }
    .about-offwork {
      gap: 0.65rem;
    }
    .about-offwork-tag {
      min-height: 1.8rem;
      font-size: 0.72rem;
    }
    .about-offwork p {
      font-size: 0.94rem;
      line-height: 1.82;
    }
    .about-section-h2 {
      margin-top: 2.2rem !important;
      margin-bottom: 0.9rem !important;
      padding-bottom: 0.45rem !important;
      padding-left: var(--about-mobile-inset-left) !important;
      padding-right: var(--about-mobile-inset-right) !important;
    }
    .about-skills {
      gap: 0.9rem;
      padding-left: var(--about-mobile-inset-left);
      padding-right: var(--about-mobile-inset-right);
    }
    .about-now-list {
      gap: 0.55rem;
      padding-left: var(--about-mobile-inset-left) !important;
      padding-right: var(--about-mobile-inset-right) !important;
      padding-right: calc(var(--about-mobile-inset-right) + 0.55rem) !important;
    }
    .about-now-list li {
      position: relative;
      overflow: hidden;
      display: grid;
      grid-template-columns: 1.1rem 1fr;
      align-items: start;
      column-gap: 0.8rem;
      margin-left: 0.55rem;
      padding: 0.9rem 1rem 0.9rem 1rem;
      border-radius: 10px;
      font-size: 0.92rem;
      line-height: 1.7;
      background: linear-gradient(135deg, var(--bg-card) 0%, var(--bg-subtle) 100%);
      box-shadow: 0 1px 0 rgba(255, 255, 255, 0.55) inset;
    }
    .about-now-text {
      display: block;
      min-width: 0;
      word-break: normal;
      overflow-wrap: anywhere;
    }
    .about-page .skill-row { flex-direction: column; gap: 0.3rem; }
    .about-page .skill-cat {
      min-width: unset;
      font-size: 0.68rem;
      letter-spacing: 0.09em;
    }
    .about-page .skill-tags {
      gap: 0.38rem;
    }
    .about-page .tag-pill,
    .about-page .ptag {
      border-radius: 999px;
      padding-left: 0.55rem;
      padding-right: 0.55rem;
    }
    .about-projects {
      gap: 0.8rem;
      padding-left: var(--about-mobile-inset-left);
      padding-right: var(--about-mobile-inset-right);
    }
    .about-page .project-card {
      position: relative;
      overflow: hidden;
      padding: 1.1rem 1.1rem 1.05rem;
      border-left-width: 2px;
      border-radius: 12px;
      background: linear-gradient(180deg, var(--bg-card) 0%, var(--bg-subtle) 100%);
      box-shadow: 0 10px 24px rgba(26, 46, 26, 0.05);
    }
    .about-page .project-card::before {
      content: "";
      position: absolute;
      top: 0;
      right: 0;
      width: 4.75rem;
      height: 4.75rem;
      background: radial-gradient(circle, rgba(74, 158, 106, 0.14) 0%, rgba(74, 158, 106, 0) 72%);
      pointer-events: none;
    }
    .about-page .project-header {
      flex-direction: column;
      align-items: flex-start;
      gap: 0.35rem;
      margin-bottom: 0.5rem;
    }
    .about-page .project-name {
      font-size: 1.02rem;
      line-height: 1.35;
    }
    .about-page .project-badge {
      padding: 0.2rem 0.45rem;
      border-radius: 999px;
      background: var(--bg-card);
    }
    .about-page .project-desc {
      font-size: 0.9rem;
      line-height: 1.75;
      margin-bottom: 0.75rem;
    }
    .about-page .project-tags,
    .about-page .skill-tags,
    .about-links {
      gap: 0.4rem;
    }
    .about-links {
      padding-left: var(--about-mobile-inset-left);
      padding-right: var(--about-mobile-inset-right);
    }
    .about-page .project-links {
      flex-wrap: wrap;
      gap: 0.55rem 0.8rem;
      padding-top: 0.15rem;
      border-top: 1px solid rgba(74, 158, 106, 0.12);
    }
    .about-page .tag-pill,
    .about-page .link-chip {
      padding-top: 0.42rem;
      padding-bottom: 0.42rem;
    }
    .about-page .project-link {
      display: inline-flex;
      align-items: center;
      min-height: 2rem;
      font-size: 0.79rem;
    }
    .about-page .link-chip {
      min-height: 2.25rem;
      border-radius: 999px;
      padding-left: 0.78rem;
      padding-right: 0.78rem;
    }
  }

  @media (max-width: 400px) {
    .about-page {
      padding-left: 0.6rem;
      padding-right: 0.6rem;
    }
    .about-body {
      --about-mobile-inset-left: 1.1rem;
      --about-mobile-inset-right: 0.35rem;
      padding: 1.05rem 1.3rem 1.45rem;
      border-radius: 14px;
    }
    .about-sidebar-blurb {
      font-size: 0.83rem;
      margin-bottom: 0.8rem;
    }
    .content .about-h1 {
      font-size: 1.85rem;
    }
    .about-body p,
    .about-intro-lede {
      font-size: 0.97rem !important;
      line-height: 1.92 !important;
    }
    .about-prose {
      max-width: 28rem;
      margin-left: auto;
      margin-right: auto;
      padding-left: var(--about-mobile-inset-left);
      padding-right: var(--about-mobile-inset-right);
    }
    .about-now-list {
      padding-right: calc(var(--about-mobile-inset-right) + 0.4rem) !important;
    }
    .about-now-list li {
      margin-left: 0.45rem;
      grid-template-columns: 1rem 1fr;
      column-gap: 0.7rem;
      padding: 0.85rem 0.9rem 0.85rem 0.9rem;
    }
    .about-timeline-item {
      padding-left: 2.2rem;
    }

    .about-timeline-item:not(:last-child) {
      margin-bottom: 0.95rem;
    }
    .about-timeline-content {
      margin-left: 0.7rem;
      padding: 0.8rem 0.9rem 0.85rem;
    }
    .about-timeline-year {
      margin-left: 0.35rem;
    }
  }
</style>

<script>
  // Dark mode toggle
  const btn = document.getElementById('theme-toggle');
  btn?.addEventListener('click', () => {
    const isDark = document.documentElement.getAttribute('data-theme') === 'dark';
    document.documentElement.setAttribute('data-theme', isDark ? 'light' : 'dark');
    localStorage.setItem('theme', isDark ? 'light' : 'dark');
  });

  // Reading progress bar + scroll-to-top with percentage
  const bar = document.getElementById('progress-bar');
  const scrollBtn = document.getElementById('scroll-top');
  const scrollPct = document.getElementById('scroll-pct');
  const SCROLL_VISIBLE_THRESHOLD = 300;

  function updateScroll() {
    const max = document.body.scrollHeight - window.innerHeight;
    if (max <= 0) return;
    const pct = Math.round((window.scrollY / max) * 100);
    if (bar) bar.style.width = `${pct}%`;
    if (scrollPct) scrollPct.textContent = `${pct}%`;
    if (scrollBtn) {
      scrollBtn.classList.toggle('visible', window.scrollY > SCROLL_VISIBLE_THRESHOLD);
    }
  }

  window.addEventListener('scroll', updateScroll, { passive: true });
  updateScroll();

  scrollBtn?.addEventListener('click', () => {
    window.scrollTo({ top: 0, behavior: 'smooth' });
  });
</script>
