---
// 相容導向：模型供應商管理已併入 Agent Console（同一個 ProviderManagement 元件，
// 且 console 版多了 /providers/[id] 詳情頁）。保留此路由避免舊書籤失效。
export const prerender = false;
return Astro.redirect('/admin/console/providers', 301);
---
